# Session Memory — 2026-06-04

## Project
book-translator — Python CLI tool: EPUB → parsed JSON → translated JSON → EPUB.

## Completed This Session

### Debug fix: [TRANSLATION FAILED] messages (primary fix)
- `translate_batch()` silently swallowed non-retryable 400 errors from providers that reject `response_format=json_schema`
- Added one-shot fallback: 400 with unsupported-response-format → retry without `response_format`
- Non-retryable 4xx now raises `TranslationError` instead of returning `{}`

### Reviewer cleanup follow-up (second pass)
1. Removed duplicate `_is_translatable` from `engine.py`; imported from `chunker.py`
2. Narrowed heuristic `_is_unsupported_response_format`; matches `"response_format"`, `"json_schema"`, `"structured output"` only
3. `use_structured_output` flag hoisted outside tenacity retry loop

### Second root cause fix + --debug flag (third pass)
- `tencent/hy3-preview` via OpenRouter returns 200 OK with `content=None` when `response_format` sent
- `translate_batch`: null/whitespace content fallback; diagnostic logging added
- CLI `--debug` flag; `_report_debug_failures()` helper

### Remove structured output / response_format (fourth pass)

**Rationale:** Many models don't support `response_format=json_schema`. Schema already injected into system prompt via `<json_schema>` tags.

**Removed from `engine.py`:**
- `_is_unsupported_response_format()` deleted
- `_create_completion()` `structured_output` param removed; no longer sends `response_format`
- `use_structured_output` flag and fallback retry logic deleted from `translate_batch()`
- `TRANSLATION_RESPONSE_FORMAT` removed from import (still defined in `prompt.py` for schema injection)

**Removed from `test_translator.py`:**
- `_make_unsupported_response_format_error()` and `_make_status_error()` helpers
- `test_translate_batch_sends_structured_response_format`
- `test_translate_batch_falls_back_when_model_rejects_structured_output`
- 6x `_is_unsupported_response_format` heuristic tests
- `test_translate_batch_remembers_fallback_on_subsequent_retry_attempts`
- 3x null/whitespace fallback tests
- Fixed `call_count == 2` → `call_count == 1` in empty/None content tests

**Added/strengthened prompt tests:**
- `test_system_prompt_contains_json_schema_tags`
- `test_system_prompt_contains_translations_schema_field`
- `test_system_prompt_contains_json_output_guidance`

## Test Status
- `tests/test_translator.py tests/test_cli.py` → **80 passed**
- Full suite blocked by missing `markdown` dep under Python 3.14 (known, unrelated)
- Ruff clean on all changed files

## Changed Files (latest pass)
- `src/book_translator/translator/engine.py` — removed structured output logic
- `tests/test_translator.py` — removed structured-output tests; added 3 prompt schema tests

## Current State
- Translator: no structured output sent; schema in system prompt; all models supported
- Phase 07 (mode-selection/CLI dispatch) in planning
