# Session Memory — 2026-06-02

## Phase 5 CLI Discussion

Created Phase 5 CLI discussion artifacts (no code written):
- `.planning/phases/05-cli/05-CONTEXT.md` — phase boundary, upstream contracts, decisions D-01..D-21, risks, success criteria
- `.planning/phases/05-cli/05-DISCUSSION-LOG.md` — date, discussed areas, decision table, deferred items, scout notes
- `.planning/STATE.md` — updated status to "Phase 5 Discussed — Ready to Plan Phase 5"

## Locked Decisions Summary (D-01..D-21)

- **D-01**: Single `translate` command; no step subcommands v1
- **D-02**: `JobStore()` base `~/.local/share/book-translator/runs`
- **D-03**: CLI copies input + parsed JSON into `job_dir/src/`
- **D-04/D-16**: Suffix validation before run creation; `.epub .txt .md .markdown`
- **D-05**: Success output: concise plain text; output EPUB path
- **D-06**: `--verbose` shows step logs + non-secret settings
- **D-07/D-08**: No JSON output; no Rich decorative output
- **D-09/D-10/D-12**: Key/base_url resolution chains; no prompt; hint on failure
- **D-11**: `meta.json` no secrets
- **D-13**: Exit codes 0/1/2
- **D-14/D-15**: Retain partial job dir on failure; concise error + hint
- **D-17**: `--output PATH`; copy/move EPUB after success
- **D-18**: Auto-delete successful run after EPUB placed
- **D-19**: `--cleanup` removes failed+completed runs; skips running/unknown
- **D-20**: `list` command for preserved runs
- **D-21**: Run state: running/failed/completed/unknown in `meta.params` or extended model

## Scout Notes
- `cli.py` missing; `pyproject.toml` entry point already declared
- `typer>=0.9` and `rich>=13.0` already in deps
- `translator/` still untracked in git; confirm public API before planning
- `markdown` missing dep caveat from 2026-06-01 — verify before Phase 5 impl

## Next Steps
- Plan Phase 5 (waves TBD by planner)
- Verify `markdown` dep / translator public API signature
- Decide `--cleanup` as root flag vs sub-command (Typer convention favors sub-command)
