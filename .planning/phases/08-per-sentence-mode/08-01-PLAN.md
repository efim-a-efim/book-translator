---
phase: 08-per-sentence-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/book_translator/translator/chunker.py
  - tests/test_translator.py
autonomous: true
requirements:
  - SENT-01
  - SENT-02
  - SENT-03
  - SENT-04
  - SENT-05
must_haves:
  truths:
    - "SENT-01: Paragraphs are split into sentences using Punkt tokenizer"
    - "SENT-02: Punkt data is auto-downloaded on first use if missing"
    - "SENT-03: ≤4-word sentences merge into preceding chunk"
    - "SENT-04: Chunks never exceed 3 sentences"
    - "SENT-05: Headers/sub-headers are never sentence-split"
  artifacts:
    - path: "src/book_translator/translator/chunker.py"
      provides: "SentenceChunk dataclass and build_sentence_chunks function"
      contains: "SentenceChunk"
    - path: "tests/test_translator.py"
      provides: "Sentence chunking unit tests"
      contains: "sentence"
---

<objective>
Implement Punkt-based sentence chunking with merge rules and header preservation.

Purpose: Create the sentence-level translation unit model and chunking logic that respects the 3-sentence chunk limit and 4-word merge rule.
Output: SentenceChunk dataclass, build_sentence_chunks() function, Punkt auto-download, and unit tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SentenceChunk dataclass and chunking logic</name>
  <files>src/book_translator/translator/chunker.py</files>
  <read_first>
    @.planning/phases/08-per-sentence-mode/08-CONTEXT.md
    @src/book_translator/translator/chunker.py
    @src/book_translator/models/document.py
  </read_first>
  <behavior>
    - SentenceChunk has id, text, paragraph_id, is_heading fields
    - build_sentence_chunks(doc) returns list[SentenceChunk]
    - Headers/sub-headers are emitted as single SentenceChunk with is_heading=True
    - Paragraphs are split into sentences using Punkt
    - ≤4-word sentences merge into preceding chunk
    - No chunk exceeds 3 sentences
    - Punkt data auto-downloaded on first use
  </action>
  <verify>
    <automated>./venv/bin/pytest tests/test_translator.py -q -k "sentence"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sentence chunking unit tests</name>
  <files>tests/test_translator.py</files>
  <read_first>
    @.planning/phases/08-per-sentence-mode/08-CONTEXT.md
    @tests/test_translator.py
    @src/book_translator/translator/chunker.py
  </read_first>
  <behavior>
    - Test Punkt auto-download behavior
    - Test header preservation (no splitting)
    - Test 4-word sentence merge rule
    - Test 3-sentence chunk limit
    - Test mixed paragraphs with headings
  </action>
  <verify>
    <automated>./venv/bin/pytest tests/test_translator.py -q -k "sentence"</automated>
  </verify>
</task>

</tasks>