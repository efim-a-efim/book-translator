# 06-04-PLAN: Error Audit, Test Gap Audit, Final Regression

**Wave:** 4  
**Depends on:** Waves 1-3

## Steps

1. Audit CLI user-facing error messages in `src/book_translator/cli.py`
2. Audit engine errors in translator, parsers for clarity
3. Fix any clearly bad messages (wording only)
4. Scan test files for skipped/xfail accumulation
5. Run final regression: `pytest -q` + `ruff check` + `ruff format --check`
6. Write `06-VERIFICATION.md`

## Commit

`fix(cli): improve user-facing error message wording` (if changes needed)  
`docs(planning): phase 6 verification`
