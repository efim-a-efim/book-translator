---
phase: 13-single-command-ephemeral-cli
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - tests/conftest.py
  - tests/test_models.py
  - tests/test_job_store.py
  - tests/test_cli.py
  - tests/test_ephemeral.py
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05, RUN-01, RUN-02, RUN-03, RUN-04, RUN-05, RUN-06]
must_haves:
  truths:
    - "The full test suite collects and passes against the rewritten single-command CLI (no references to deleted JobStore/JobMeta/STATE_*)"
    - "Tests invoke the root command directly with no 'translate' token (CLI-01)"
    - "A test proves the run dir is created under tempfile.gettempdir() with prefix book-translator- (RUN-01)"
    - "A test proves the run dir is deleted on success and the output EPUB lands at the destination (RUN-03)"
    - "A test proves the run dir is deleted on failure with exit 1 (RUN-04)"
    - "A test proves --preserve-temp retains the dir and prints the preserved line on success and failure (RUN-05/RUN-06)"
    - "A test proves --debug retains the dir and prints the (--debug implies --preserve-temp) annotation (RUN-06)"
    - "A test proves the path is not printed on a clean default run and IS printed under --verbose/--debug/--preserve-temp (RUN-02/D-05)"
    - "A test proves --help has no Commands section and shows the INPUT argument + options (CLI-05)"
    - "A test proves 'list'/'cleanup' run no list/cleanup logic and exit non-zero as an invalid input path (CLI-03/CLI-04, Pitfall 1)"
  artifacts:
    - path: "tests/test_ephemeral.py"
      provides: "behavioral tests for the ephemeral run-dir lifecycle (RUN-01..06, CLI-05, CLI-03/04)"
      contains: "book-translator-"
    - path: "tests/test_job_store.py"
      provides: "DELETED — store removed (D-04)"
    - path: "tests/test_cli.py"
      provides: "rewritten CLI-invocation tests with no 'translate' token and no store/state assertions"
  key_links:
    - from: "tests/test_ephemeral.py"
      to: "book_translator.cli.app"
      via: "CliRunner().invoke(app, [str(input), ...]) + monkeypatch tempfile.mkdtemp to a known dir"
      pattern: "mkdtemp|CliRunner"
    - from: "tests/test_cli.py"
      to: "book_translator.cli.app"
      via: "invoke without the 'translate' positional token"
      pattern: "invoke\\(app, \\[str"
---

<objective>
Rewrite the test suite to match the single-command ephemeral CLI from Plan 01: delete obsolete store/list/cleanup/meta.json tests, drop the `translate` subcommand token, and add behavioral tests proving the ephemeral run-dir lifecycle (RUN-01..06) and the new CLI surface (CLI-01..05).

Purpose: Lock the phase requirements behind automated tests so the new single-command, no-state-left-behind behavior is verifiable and regression-proof.

Output: Deleted `tests/test_job_store.py`; edited `tests/conftest.py`, `tests/test_models.py`, `tests/test_cli.py`; new `tests/test_ephemeral.py`. Full `pytest` passes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-single-command-ephemeral-cli/13-CONTEXT.md
@.planning/phases/13-single-command-ephemeral-cli/13-RESEARCH.md
@.planning/phases/13-single-command-ephemeral-cli/13-PATTERNS.md
@.planning/phases/13-single-command-ephemeral-cli/13-01-PLAN.md
@src/book_translator/cli.py
@tests/test_cli.py
@tests/conftest.py
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

**Created:**
- `tests/test_ephemeral.py` — behavioral tests for the ephemeral lifecycle. Strategy: monkeypatch `book_translator.cli.tempfile.mkdtemp` (or `tempfile.mkdtemp`) to return a known dir under pytest `tmp_path`; assert existence/absence post-run and parse `Run directory:` from `result.output`. Mocks the async engine (`AsyncMock`/`patch`) so tests never hit the network.

**Edited:**
- `tests/conftest.py` — removed the `store` fixture + its `from book_translator.store.job_store import JobStore` import (D-04).
- `tests/test_models.py` — removed `from book_translator.models.job import JobMeta` (L6) and the `JobMeta` test (L68); kept BookDocument/Chapter/Paragraph tests.
- `tests/test_cli.py` — removed the store import block (L10-16) and the `tmp_store` fixture (L24-30); dropped the literal `"translate"` first arg from every `runner.invoke`; deleted all list/cleanup/meta.json/STATE_* tests; re-targeted granularity/mode tests to assert behavior (not meta.json contents).

**DELETED:**
- File `tests/test_job_store.py` (8 tests targeting the removed `JobStore`/`JobMeta`).
- `tests/conftest.py` `store` fixture.
- `tests/test_cli.py` `tmp_store` fixture, the `~9` list/cleanup subcommand tests, and the store/state/meta.json assertion tests.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Delete obsolete store tests and prune store/JobMeta references</name>
  <files>tests/test_job_store.py, tests/conftest.py, tests/test_models.py</files>
  <read_first>
    - tests/test_job_store.py, tests/conftest.py, tests/test_models.py (files being deleted/edited)
    - .planning/phases/13-single-command-ephemeral-cli/13-RESEARCH.md § "Blast Radius" → "Test files — DELETE / EDIT"
    - .planning/phases/13-single-command-ephemeral-cli/13-PATTERNS.md § "tests/conftest.py" and § "tests/test_models.py"
    - .planning/phases/13-single-command-ephemeral-cli/13-CONTEXT.md § D-04
  </read_first>
  <action>
    Delete `tests/test_job_store.py` entirely (all 8 tests target the removed `JobStore`/`JobMeta`).

    In `tests/conftest.py`: remove the `from book_translator.store.job_store import JobStore` import and the `store` fixture (it is the only fixture; the file may become effectively empty — that is fine, or repurpose it to hold the new ephemeral `mkdtemp`-monkeypatch fixture if you prefer one shared fixture, but `test_ephemeral.py` may define its own). Leave no reference to `book_translator.store`.

    In `tests/test_models.py`: remove the `from book_translator.models.job import JobMeta` import (L6) and the `JobMeta` test at L68. Keep all BookDocument/Chapter/Paragraph tests unchanged.
  </action>
  <verify>
    <automated>test ! -e tests/test_job_store.py && ! grep -rnE "book_translator\.store|book_translator\.models\.job|JobStore|JobMeta" tests/conftest.py tests/test_models.py && python -m pytest tests/test_models.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e tests/test_job_store.py` passes.
    - `grep -nE "JobStore|JobMeta|book_translator\.store|book_translator\.models\.job" tests/conftest.py tests/test_models.py` returns nothing.
    - `python -m pytest tests/test_models.py -q` exits 0 (BookDocument/Chapter/Paragraph tests still pass).
  </acceptance_criteria>
  <done>test_job_store.py is deleted; conftest.py and test_models.py have no store/JobMeta references; test_models.py passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite test_cli.py and add test_ephemeral.py for the single-command ephemeral surface</name>
  <files>tests/test_cli.py, tests/test_ephemeral.py</files>
  <behavior>
    - CLI-01: invoke(app, [str(input), "--source-lang","en","--target-lang","ru"]) (no "translate" token, engine mocked) succeeds.
    - CLI-02: invoking with --model/--concurrency/--max-retries/etc. parses on the root command with no parse error; --preserve-temp is accepted.
    - CLI-03/04: invoke(app, ["list"]) and invoke(app, ["cleanup"]) run NO list/cleanup logic and exit non-zero as an invalid input path (do NOT assert a Click "no such command" string — Pitfall 1).
    - CLI-05: invoke(app, ["--help"]) → "Commands" not in output; INPUT argument + options present.
    - RUN-01: with mkdtemp monkeypatched, the created run dir is under tempfile.gettempdir() and uses prefix "book-translator-".
    - RUN-02/D-05: default run → "Run directory:" not in output; with --verbose / --debug / --preserve-temp → "Run directory:" present.
    - RUN-03: success (engine mocked) → captured run dir does not exist after run; output EPUB exists at dest; exit 0.
    - RUN-04: engine mocked to raise TranslationError → captured run dir deleted; exit 1.
    - RUN-05: --preserve-temp (and separately --debug) → captured run dir still exists after run, on BOTH success and failure paths.
    - RUN-06: --preserve-temp → "Run directory preserved:" present; --debug → also "(--debug implies --preserve-temp)" present.
  </behavior>
  <read_first>
    - tests/test_cli.py (the file being rewritten — current fixtures + mock pattern to reuse)
    - src/book_translator/cli.py (the rewritten root command from Plan 01 — exact printed strings + exit codes)
    - .planning/phases/13-single-command-ephemeral-cli/13-RESEARCH.md § "Validation Architecture" (per-requirement test strategy + "Capturing the run dir path") and § "Common Pitfalls 1" (list/cleanup assertion)
    - .planning/phases/13-single-command-ephemeral-cli/13-PATTERNS.md § "tests/test_cli.py" and § "tests/test_ephemeral.py" (the 8-test checklist + mkdtemp-monkeypatch hook)
    - .planning/phases/13-single-command-ephemeral-cli/13-CONTEXT.md § D-05, D-10, D-11, D-13
  </read_first>
  <action>
    In `tests/test_cli.py`: remove the `from book_translator.store.job_store import (...)` block (L10-16) and the `tmp_store` fixture (L24-30, which monkeypatched the deleted `RUNS_BASE`/`cli.JobStore`). Keep the `runner` fixture, the `sample_txt` fixture, and the `from unittest.mock import AsyncMock, MagicMock, patch` pattern. In every remaining `runner.invoke(app, ["translate", str(f), ...])` call, drop the `"translate"` token so it becomes `runner.invoke(app, [str(f), ...])` (CLI-01). Delete every list/cleanup subcommand test and every test asserting `meta.json` contents, `STATE_*`, or `tmp_store.iterdir()` (these target removed persistence — D-04). Re-target the granularity/mode tests (e.g. `test_page_granularity_metadata`, `test_mode_metadata*`, `test_mode_interactive_dispatches_assemble_interactive`, `test_mode_monolingual_dispatches_assemble_monolingual`, `test_mode_omitted_defaults_to_parallel`) to assert dispatch behavior (which assemble* was called, via the existing `patch`/mock) or output, NOT meta.json — delete any test that only checked persisted metadata. Keep all validation tests (unsupported suffix, missing file, missing required options, invalid granularity/mode exit-2, batch-token-budget rejection) but drop their `"translate"` token and any `tmp_store.iterdir()` assertion.

    Create `tests/test_ephemeral.py` covering the 10 behaviors above. Capture the run dir by monkeypatching `book_translator.cli.tempfile.mkdtemp` to return a known directory under pytest `tmp_path` (create it in the patch so `(job_dir/'src').mkdir()` succeeds), then assert `Path(captured).exists()` is False after a default/failure run and True after a preserve run. For RUN-01, assert the real (un-monkeypatched) `mkdtemp` path starts with `tempfile.gettempdir()` and the basename starts with `book-translator-` (or assert the prefix kwarg passed to a spy). Mock the async engine with `AsyncMock`/`patch` (reuse the existing test_cli.py mock targets for `translate`/`translate_sentence`/`assemble*`) so no test hits the network. For CLI-03/04 assert `result.exit_code != 0` and that no list/cleanup output text appears — do NOT assert "No such command". For CLI-05 assert `"Commands" not in result.output` and the INPUT argument + an option (e.g. `--source-lang`) appear; assert on structure, NOT the literal program name (CliRunner shows the function name).
  </action>
  <verify>
    <automated>! grep -nE "\"translate\"|'translate'|JobStore|JobMeta|STATE_|tmp_store|meta\.json|invoke\(app, \[\"list\"|invoke\(app, \[\"cleanup\"" tests/test_cli.py && grep -q "book-translator-" tests/test_ephemeral.py && grep -q "preserved" tests/test_ephemeral.py && python -m pytest tests/test_cli.py tests/test_ephemeral.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "\"translate\"|'translate'" tests/test_cli.py` returns nothing (CLI-01: no subcommand token).
    - `grep -nE "JobStore|JobMeta|STATE_|tmp_store|meta\.json" tests/test_cli.py` returns nothing (store/state tests removed).
    - `grep -n "invoke(app, \[\"list\"\|invoke(app, \[\"cleanup\"" tests/test_cli.py` finds no surviving subcommand-dispatch tests in test_cli.py (any list/cleanup behavior assertion lives in test_ephemeral.py as a non-zero-exit invalid-input check).
    - `tests/test_ephemeral.py` exists and contains `book-translator-` (RUN-01 prefix), `preserved` (RUN-06), and `(--debug implies --preserve-temp)`.
    - `python -m pytest tests/test_cli.py tests/test_ephemeral.py -q` exits 0.
    - test_ephemeral.py asserts: run dir under `tempfile.gettempdir()`; deleted on success; deleted on failure with exit 1; retained with `--preserve-temp` and `--debug`; path not printed on default run but printed under `--verbose`/`--debug`/`--preserve-temp`; `--help` has no "Commands" section.
  </acceptance_criteria>
  <done>test_cli.py invokes the root command with no "translate" token and has no store/state/list/cleanup assertions; test_ephemeral.py proves RUN-01..06 + CLI-03/04/05 behaviorally; both files pass under pytest.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite + ruff green</name>
  <files>tests/test_cli.py, tests/test_ephemeral.py, tests/conftest.py, tests/test_models.py</files>
  <read_first>
    - .planning/phases/13-single-command-ephemeral-cli/13-RESEARCH.md § "Runtime State Inventory" (reinstall / clear __pycache__ note after deleting modules) and § "Project Constraints" (ruff config)
  </read_first>
  <action>
    Run the full test suite and the linter. Clear stale bytecode for the deleted modules if collection errors reference them (`find . -name '__pycache__' -path '*book_translator*' -prune` cleanup, or reinstall the editable package so the removed modules are no longer shipped — RESEARCH Runtime State Inventory). Fix any remaining failures or ruff findings introduced by the test rewrite. Do not modify `src/` — if a failure indicates a source defect, it is out of this plan's scope and must be reported in the SUMMARY (Plan 01 owns `src/`).
  </action>
  <verify>
    <automated>python -m pytest -q && ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q` exits 0 (entire suite green, no collection errors referencing the deleted modules).
    - `ruff check src tests` passes.
    - No remaining test references the deleted `JobStore`/`JobMeta`/`STATE_*` symbols anywhere under `tests/`.
  </acceptance_criteria>
  <done>Full pytest suite and ruff pass across src and tests; no stale references to removed persistence symbols remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → CLI under test | CliRunner invokes the in-process app with crafted args; mocked engine prevents real network calls |
| Test → OS temp filesystem | Monkeypatched mkdtemp targets pytest `tmp_path`, isolating test run dirs from the real `$TMPDIR` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Information Disclosure | Tests hitting the real translation API / leaking keys | mitigate | All engine calls mocked with `AsyncMock`/`patch` (reused pattern); no test reads real API keys or performs network I/O. |
| T-13-02-02 | Tampering | Tests deleting/inspecting the real system temp dir | mitigate | mkdtemp monkeypatched to a pytest `tmp_path` subdir; cleanup assertions operate only on the isolated path, never the shared `$TMPDIR`. |
| T-13-02-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this plan (stdlib + already-installed pytest/typer). RESEARCH Package Legitimacy Audit: N/A. |

No HIGH-severity threats — test-only changes with mocked I/O. Block-on-high gate has nothing to block.
</threat_model>

<verification>
- `python -m pytest -q` passes (full suite, no collection errors).
- `ruff check src tests` passes.
- No `tests/` reference to `JobStore`, `JobMeta`, `STATE_*`, `tmp_store`, `meta.json`, or a `"translate"` invoke token.
- `tests/test_ephemeral.py` proves RUN-01..06 and CLI-03/04/05 behaviorally.
</verification>

<success_criteria>
- Obsolete store/list/cleanup/meta.json tests removed; `tests/test_job_store.py` deleted.
- Root command invoked with no `translate` token everywhere (CLI-01).
- Ephemeral lifecycle (RUN-01..06) and CLI surface (CLI-03/04/05) covered by passing behavioral tests.
- Full pytest + ruff green across `src` and `tests`.
</success_criteria>

<output>
Create `.planning/phases/13-single-command-ephemeral-cli/13-02-SUMMARY.md` when done.
</output>
