---
phase: 12-css-cli-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/book_translator/assembler/builder.py
  - tests/test_builder.py
autonomous: true
requirements:
  - INTR-13
  - INTR-14
  - INTR-15
  - INTR-16
  - INTR-17

must_haves:
  truths:
    - "build_interactive() produces an EPUB whose style.css contains the literal bytes for CSS escape \\25B6 (not the raw ▶ character)"
    - "The EPUB style.css contains all three disclosure-triangle-hiding rules: list-style:none, ::-webkit-details-marker display:none, ::marker display:none"
    - "span.bt-heading-translation CSS rules set display:block, font-size:0.6em, opacity:0.5, font-style:italic"
    - "build() and build_monolingual() still produce empty CSS (b'' bytes) — _INTERACTIVE_CSS does not propagate"
  artifacts:
    - path: "src/book_translator/assembler/builder.py"
      provides: "_INTERACTIVE_CSS module-level constant; build_interactive() wired to pass _INTERACTIVE_CSS.encode('utf-8') to _make_css_item()"
      contains: "_INTERACTIVE_CSS"
  key_links:
    - from: "builder.py _INTERACTIVE_CSS"
      to: "_make_css_item(content=...)"
      via: "_INTERACTIVE_CSS.encode('utf-8')"
      pattern: "_make_css_item\\(content=_INTERACTIVE_CSS\\.encode"
---

<objective>
Define the `_INTERACTIVE_CSS` module-level constant in `builder.py` and wire it into `build_interactive()` so that the interactive EPUB receives a real stylesheet instead of the stub `b""`.

Purpose: Satisfies INTR-13 (CSS bundled, no script tags), INTR-14 (disclosure triangle hidden), INTR-15 (Unicode escapes used), INTR-16 (UTF-8 bytes passed to EpubItem), INTR-17 (heading span visually subordinate). Implements decisions D-06 through D-12.

Output: Updated `builder.py` with CSS constant; updated `tests/test_builder.py` with CSS content assertions.
</objective>

<execution_context>
@/Users/aefimov/.claude/gsd-core/workflows/execute-plan.md
@/Users/aefimov/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-css-cli-integration/12-CONTEXT.md
@.planning/phases/12-css-cli-integration/12-RESEARCH.md
@.planning/phases/12-css-cli-integration/12-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _INTERACTIVE_CSS constant and wire into build_interactive()</name>
  <files>src/book_translator/assembler/builder.py</files>
  <read_first>
    - src/book_translator/assembler/builder.py — current _make_css_item() signature (line 18), current build_interactive() stub call (line 228 — css_item = _make_css_item()), existing imports and module structure
    - .planning/phases/12-css-cli-integration/12-CONTEXT.md — decisions D-06 through D-12 (exact CSS values and organization rules)
    - .planning/phases/12-css-cli-integration/12-RESEARCH.md — Full _INTERACTIVE_CSS constant code example, Pitfall 1 (Python string escape \\25B6 vs \25B6), Pitfall 2 (selector specificity for [open] rule)
    - .planning/phases/12-css-cli-integration/12-PATTERNS.md — Pattern Assignment for builder.py (exact constant text and wire pattern)
  </read_first>
  <behavior>
    - _INTERACTIVE_CSS is a module-level string constant defined after imports, before EpubBuilder class
    - _INTERACTIVE_CSS contains `content: "\\25B6"` (double backslash in Python source) so the literal CSS on disk reads `content: "\25B6"` — single backslash CSS hex escape (INTR-15, Pitfall 1)
    - _INTERACTIVE_CSS contains `content: "\\25BC"` for expanded state — same escaping rule
    - _INTERACTIVE_CSS includes the three disclosure-triangle rules (D-07): `summary.bt-original { list-style: none }`, `summary.bt-original::-webkit-details-marker { display: none }`, `summary.bt-original::marker { display: none }`
    - _INTERACTIVE_CSS includes `span.bt-heading-translation { display: block; font-size: 0.6em; opacity: 0.5; font-style: italic }` (D-06, INTR-17)
    - _INTERACTIVE_CSS expanded-arrow selector is `details[open].bt-interactive > summary.bt-original::before` placed AFTER the collapsed rule (D-08, Pitfall 2)
    - In build_interactive(): replace `css_item = _make_css_item()` with `css_item = _make_css_item(content=_INTERACTIVE_CSS.encode("utf-8"))` (D-11, INTR-16)
    - build() line 71 and build_monolingual() line 146 still call `_make_css_item()` with no args — do NOT change them
    - No other builders receive _INTERACTIVE_CSS
  </behavior>
  <action>
    Add `_INTERACTIVE_CSS` as a module-level string constant in builder.py immediately after the import block and before the `_make_css_item` function definition. The constant must use `\\25B6` and `\\25BC` in the Python source (double backslash) so the CSS file receives the literal `\25B6` / `\25BC` sequences. Place the `details[open].bt-interactive > summary.bt-original::before` rule AFTER the base `summary.bt-original::before` rule in source order to ensure specificity wins (per D-08 and Pitfall 2). Include empty rule blocks for `details.bt-interactive {}` (D-09: no border/background) and `.bt-translation {}` (D-10: no custom color). Include `cursor: pointer` on `summary.bt-original`. Include a small `margin-right: 0.3em` on `summary.bt-original::before`. Add `margin-bottom` on `.bt-translation` at Claude's discretion for paragraph spacing.

    In `build_interactive()`, locate the single line `css_item = _make_css_item()` (currently line 228) and change it to `css_item = _make_css_item(content=_INTERACTIVE_CSS.encode("utf-8"))`. Do not touch the identical call in `build()` or `build_monolingual()`.
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -c "from book_translator.assembler.builder import _INTERACTIVE_CSS; assert '\\\\25B6' not in _INTERACTIVE_CSS, 'double-backslash leaked into CSS'; assert chr(0x25B6) not in _INTERACTIVE_CSS, 'raw arrow char must not appear'; assert '\\25B6' in _INTERACTIVE_CSS, 'CSS escape must be present'; print('escape check OK')"</automated>
  </verify>
  <done>_INTERACTIVE_CSS defined at module level; build_interactive() passes _INTERACTIVE_CSS.encode("utf-8") to _make_css_item(); build() and build_monolingual() unchanged; Python import succeeds; escape check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CSS content assertions to test_builder.py</name>
  <files>tests/test_builder.py</files>
  <read_first>
    - tests/test_builder.py — existing test structure, existing _make_css_item tests (line 40+), existing build_interactive tests (line 92+), existing test_build_interactive_includes_css_item test (line 84)
    - src/book_translator/assembler/builder.py — _INTERACTIVE_CSS constant (after Task 1 completes), _make_css_item() signature
    - .planning/REQUIREMENTS.md — INTR-13, INTR-14, INTR-15, INTR-16, INTR-17 requirement text for assertion rationale
  </read_first>
  <behavior>
    - New test: `test_interactive_css_bytes_in_build_interactive()` — calls build_interactive() with a minimal BookDocument, retrieves css_item from book.items, asserts css_item.content is bytes (INTR-16), asserts b"\\x5c25B6" (the bytes of the literal backslash-25B6 sequence) OR assert b"\\25B6" in css_item.content, asserts b"list-style: none" in css_item.content (INTR-14), asserts b"display: none" in css_item.content (INTR-14), asserts b"font-size: 0.6em" in css_item.content (INTR-17), asserts b"opacity: 0.5" in css_item.content (INTR-17)
    - New test: `test_build_does_not_include_interactive_css()` — calls build() with minimal doc, retrieves css_item, asserts css_item.content == b"" (interactive CSS must not appear in non-interactive builds)
    - New test: `test_build_monolingual_does_not_include_interactive_css()` — same as above for build_monolingual()
    - Import `_INTERACTIVE_CSS` from `book_translator.assembler.builder` in test file for direct constant assertions
    - New test: `test_interactive_css_constant_no_raw_arrow_chars()` — asserts chr(0x25B6) not in _INTERACTIVE_CSS and chr(0x25BC) not in _INTERACTIVE_CSS (INTR-15)
    - New test: `test_interactive_css_constant_has_all_triangle_hiding_rules()` — asserts "list-style: none" in _INTERACTIVE_CSS, "::-webkit-details-marker" in _INTERACTIVE_CSS, "::marker" in _INTERACTIVE_CSS (INTR-14)
  </behavior>
  <action>
    Add a new test class `TestInteractiveCSSContent` in test_builder.py after the existing `_make_css_item` tests. Use the existing conftest minimal-doc fixture or create a minimal BookDocument inline (single chapter, one paragraph). For css_item retrieval from book.items, filter by `item.media_type == "text/css"` to find the style item. Each test method is independent — no shared state beyond the minimal document fixture.
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -m pytest tests/test_builder.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>All test_builder.py tests pass including the new TestInteractiveCSSContent class; no test_builder.py failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python source → EPUB bytes | CSS constant defined in Python is encoded and embedded in EPUB; no external input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | CSS escape sequences in _INTERACTIVE_CSS | mitigate | Use `\\25B6` (double backslash) in Python string literal — the test `test_interactive_css_constant_no_raw_arrow_chars()` enforces no raw Unicode arrows reach the CSS file; ebooklib corruption of raw non-ASCII is the documented risk (INTR-15) |
| T-12-02 | Information Disclosure | No `<script>` tags in CSS | accept | CSS-only output; `_INTERACTIVE_CSS` contains no `<script>`, `javascript:`, or event handler strings; INTR-13 enforced by content definition |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; existing ebooklib + Python stdlib only |
</threat_model>

<verification>
1. `python -m pytest tests/test_builder.py -x -q` — all tests pass
2. `python -c "from book_translator.assembler.builder import _INTERACTIVE_CSS, EpubBuilder; print('import OK')"` — no ImportError
3. `grep -c "\\\\25B6" src/book_translator/assembler/builder.py` — returns ≥ 1 (double-backslash present in source)
4. `python -c "from book_translator.assembler.builder import _INTERACTIVE_CSS; assert chr(0x25B6) not in _INTERACTIVE_CSS; print('no raw arrow OK')"` — passes
5. `python -c "from book_translator.assembler.builder import _INTERACTIVE_CSS; assert 'list-style: none' in _INTERACTIVE_CSS; assert 'webkit-details-marker' in _INTERACTIVE_CSS; assert '::marker' in _INTERACTIVE_CSS; print('triangle rules OK')"` — passes
</verification>

<success_criteria>
- _INTERACTIVE_CSS constant exists in builder.py at module level
- build_interactive() passes _INTERACTIVE_CSS.encode("utf-8") to _make_css_item()
- build() and build_monolingual() still use _make_css_item() with no args (b"")
- All INTR-14 triangle-hiding rules present in constant
- INTR-15: no raw Unicode arrow characters (▶/▼) in constant; \\25B6/\\25BC used
- INTR-16: .encode("utf-8") called at _make_css_item() invocation
- INTR-17: font-size:0.6em, opacity:0.5, font-style:italic in span.bt-heading-translation block
- All test_builder.py tests pass
</success_criteria>

## Artifacts this phase produces

| Symbol | Type | File | Description |
|--------|------|------|-------------|
| `_INTERACTIVE_CSS` | module-level string constant | `src/book_translator/assembler/builder.py` | Full interactive CSS stylesheet as Python string; double-backslash Unicode escapes |
| `TestInteractiveCSSContent` | test class | `tests/test_builder.py` | CSS content assertions: escape safety, triangle-hiding rules, heading span styles, isolation from non-interactive builds |

<output>
Create `.planning/phases/12-css-cli-integration/12-01-SUMMARY.md` when done
</output>
