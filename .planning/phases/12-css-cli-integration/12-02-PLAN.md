---
phase: 12-css-cli-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/book_translator/cli.py
  - src/book_translator/assembler/__init__.py
  - tests/test_cli.py
autonomous: true
requirements:
  - INTR-03
  - INTR-04
  - INTR-05

must_haves:
  truths:
    - "translate --mode interactive completes the CLI pipeline without error (INTR-03)"
    - "translate (no --mode) defaults to per-page — no behavior change (INTR-05)"
    - "translate --mode interactive --output-format txt is rejected because --output-format option no longer exists (D-02)"
    - "translate --mode monolingual produces EPUB (D-04: no format param passed)"
    - "translate --mode per-sentence --batch-token-budget validation unchanged (D-05)"
    - "VALID_MODES contains exactly: per-page, per-sentence, monolingual, interactive (D-01)"
    - "FORMAT_TO_EXT, VALID_OUTPUT_FORMATS, and the --output-format Typer option are absent from cli.py"
  artifacts:
    - path: "src/book_translator/cli.py"
      provides: "VALID_MODES with interactive; --output-format removed; assemble_interactive dispatch branch"
      contains: "\"interactive\""
    - path: "src/book_translator/assembler/__init__.py"
      provides: "assemble_interactive() function; updated __all__"
      exports: ["assemble", "assemble_interactive", "assemble_monolingual"]
  key_links:
    - from: "cli.py effective_mode == 'interactive'"
      to: "assembler/__init__.py assemble_interactive()"
      via: "import + elif branch"
      pattern: "assemble_interactive\\(job_dir=run_dir"
---

<objective>
Wire `--mode interactive` into the CLI and assembler public surface. Simultaneously remove `--output-format` and all dead code (VALID_OUTPUT_FORMATS, FORMAT_TO_EXT) per user decision D-02. Add `assemble_interactive()` to `assembler/__init__.py` following the exact `assemble()` pattern. Update `tests/test_cli.py` to delete/update `--output-format` tests and add interactive mode tests.

Purpose: Satisfies INTR-03 (interactive is valid mode), INTR-04 (superseded by D-02 — format flag removed entirely), INTR-05 (default per-page unchanged). Implements decisions D-01 through D-05.

Output: Updated cli.py (surgical edits); updated assembler/__init__.py (new function + __all__); updated tests/test_cli.py (--output-format tests removed, interactive tests added).
</objective>

<execution_context>
@/Users/aefimov/.claude/gsd-core/workflows/execute-plan.md
@/Users/aefimov/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-css-cli-integration/12-CONTEXT.md
@.planning/phases/12-css-cli-integration/12-RESEARCH.md
@.planning/phases/12-css-cli-integration/12-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add assemble_interactive() to assembler/__init__.py</name>
  <files>src/book_translator/assembler/__init__.py</files>
  <read_first>
    - src/book_translator/assembler/__init__.py — assemble() function (lines 14–37) as the exact structural template; __all__ (line 11); assemble_monolingual() signature; existing imports
    - .planning/phases/12-css-cli-integration/12-CONTEXT.md — D-03: assemble_interactive() calls EpubBuilder().build_interactive(); D-04: context on format removal
    - .planning/phases/12-css-cli-integration/12-PATTERNS.md — Pattern Assignment for __init__.py (exact assemble_interactive() code and __all__ change)
    - src/book_translator/assembler/builder.py — build_interactive() method signature: (self, doc, target_lang, book_id="") to confirm call syntax
  </read_first>
  <behavior>
    - assemble_interactive(job_dir: Path, target_lang: str) -> Path — identical signature to assemble(); no output_format param
    - Function body is identical to assemble() except the EpubBuilder call: EpubBuilder().build_interactive(doc, target_lang, book_id=str(job_dir.name))
    - Uses atomic write pattern: tmp_path = epub_path.with_suffix(".epub.tmp"); epub.write_epub(...); os.replace(tmp_path, epub_path)
    - __all__ updated from ["assemble", "assemble_monolingual"] to ["assemble", "assemble_interactive", "assemble_monolingual"] — alphabetical insertion
    - No changes to assemble() or _assemble_monolingual_epub() behavior
    - assemble_monolingual() internal signature change is Claude's discretion — either simplify (remove output_format param + dead format branches) or leave (no runtime impact since CLI no longer passes non-epub values); choose simplification to reduce dead code
  </behavior>
  <action>
    Add `assemble_interactive` function after `assemble()` and before `assemble_monolingual()` in assembler/__init__.py. Copy the assemble() function body exactly, replacing only the `EpubBuilder().build(...)` call with `EpubBuilder().build_interactive(...)`. Update __all__ to include "assemble_interactive" between "assemble" and "assemble_monolingual".

    For assemble_monolingual() cleanup (discretion): remove the `output_format: str = "epub"` parameter, remove the `elif output_format == "txt"` and `elif output_format == "md"` branches and the `else: raise ValueError` branch, remove the private `_assemble_monolingual_txt()` and `_assemble_monolingual_md()` functions. The function body then delegates directly to `_assemble_monolingual_epub()`. This eliminates dead code without changing behavior since no caller passes non-epub values after D-02.
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -c "from book_translator.assembler import assemble, assemble_interactive, assemble_monolingual; print('import OK')"</automated>
  </verify>
  <done>assemble_interactive() function exists in assembler/__init__.py; "assemble_interactive" in __all__; import succeeds; assemble() and assemble_monolingual() still importable and callable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surgical CLI edits — add interactive, remove --output-format</name>
  <files>src/book_translator/cli.py</files>
  <read_first>
    - src/book_translator/cli.py — VALID_MODES (line 28), VALID_OUTPUT_FORMATS (line 29), FORMAT_TO_EXT (line 30), output_format Typer option (line 124), validation block (lines 159–170), output path derivation (lines 190–193), dispatch block (lines 293–297), import line (line 12)
    - .planning/phases/12-css-cli-integration/12-CONTEXT.md — D-01 (VALID_MODES), D-02 (full list of what to delete), D-03 (dispatch), D-04 (monolingual call site), D-05 (batch-token-budget unchanged)
    - .planning/phases/12-css-cli-integration/12-PATTERNS.md — CLI surgical edits section with exact before/after for each change
    - src/book_translator/assembler/__init__.py — confirm assemble_interactive is now importable (after Task 1)
  </read_first>
  <behavior>
    - VALID_MODES = {"per-page", "per-sentence", "monolingual", "interactive"} — D-01
    - Lines 29–30 deleted: VALID_OUTPUT_FORMATS and FORMAT_TO_EXT constants removed — D-02
    - Import line 12 updated: add assemble_interactive to from book_translator.assembler import statement
    - output_format Typer option parameter removed from translate_cmd() signature (line 124) — D-02
    - Lines 159–170 deleted (output_format validation block: two if-blocks checking output_format) — D-02
    - Lines 190–193 simplified: if/else replaced with `_ext = ".epub"` (single line) — D-02
    - Dispatch block (lines 293–297) updated to: if effective_mode == "monolingual": out_path = assemble_monolingual(job_dir=run_dir, target_lang=target_lang); elif effective_mode == "interactive": out_path = assemble_interactive(job_dir=run_dir, target_lang=target_lang); else: out_path = assemble(job_dir=run_dir, target_lang=target_lang) — D-03, D-04
    - line 294 (`out_format = output_format or "epub"`) deleted as part of dispatch update — D-04
    - batch_token_budget validation (lines 171–176) unchanged — D-05
    - translate_cmd() docstring updated to reflect removed option (minor update, drop "monolingual" from --output-format mention)
    - No other changes to the CLI function
  </behavior>
  <action>
    Apply the following surgical edits to cli.py in this order:

    1. Line 12 (import): add `assemble_interactive` to the `from book_translator.assembler import` statement.
    2. Line 28 (VALID_MODES): add "interactive" to the set literal.
    3. Lines 29–30 (VALID_OUTPUT_FORMATS, FORMAT_TO_EXT): delete both lines.
    4. Line 124 (output_format Typer option): delete this parameter from translate_cmd() signature.
    5. Lines 159–170 (output_format validation block): delete all lines in this block (the two if-blocks that check output_format, per D-02).
    6. Lines 190–193 (output path _ext derivation): replace the if/else block with the single line `_ext = ".epub"`.
    7. Lines 293–297 (dispatch block): replace with the three-branch if/elif/else (monolingual / interactive / else) without output_format argument per D-03 and D-04.

    Line numbers shift as edits are applied; apply top-to-bottom to keep them accurate.
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -c "from book_translator.cli import app, VALID_MODES; assert 'interactive' in VALID_MODES; assert 'FORMAT_TO_EXT' not in dir(); print('VALID_MODES:', VALID_MODES)"</automated>
  </verify>
  <done>cli.py imports cleanly; VALID_MODES contains "interactive"; VALID_OUTPUT_FORMATS and FORMAT_TO_EXT do not exist in module namespace; output_format parameter absent from translate_cmd() signature; interactive elif branch present in dispatch block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update test_cli.py — remove --output-format tests, add interactive mode tests</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py — test functions: test_output_format_rejected_without_monolingual (line 819), test_output_format_rejected_with_per_page (line 831), test_monolingual_with_output_format (line 867), lines ~957, ~1006, ~1020–1065 area (output_format references) — read the full set of output_format-touching tests to know exactly what to remove
    - tests/conftest.py — fixtures used: runner, tmp_store, sample_txt (needed for new tests)
    - src/book_translator/cli.py — updated VALID_MODES (after Task 2) to confirm "interactive" is present
    - src/book_translator/assembler/__init__.py — assemble_interactive() signature (after Task 1)
  </read_first>
  <behavior>
    - Delete (or replace with a new test) all test functions that reference `--output-format` as a valid option. Affected tests: test_output_format_rejected_without_monolingual, test_output_format_rejected_with_per_page, test_monolingual_with_output_format, test_monolingual_txt_output_gets_txt_extension, test_monolingual_md_output_gets_md_extension, test_monolingual_without_output_format_gets_epub_extension, and any helpers/fixtures that pass `output_format` to assemble_monolingual mock
    - Add replacement test: `test_output_format_option_does_not_exist()` — invokes `translate` with `["--output-format", "epub"]` via runner.invoke, asserts exit code != 0, asserts "no such option" in output (case-insensitive) OR asserts exit code == 2 (Typer unknown option)
    - Add test: `test_interactive_mode_is_valid()` — invokes translate with `["--mode", "interactive", ...]` up to the translation step with a mocked translate/assemble_interactive; asserts no "invalid mode" error, asserts assemble_interactive called; mock pattern mirrors existing per-page and monolingual test helpers
    - Add test: `test_default_mode_is_per_page()` — existing test or new assertion that omitting `--mode` results in effective_mode == "per-page"; confirm existing test coverage already handles this or add explicit test
    - Existing tests for monolingual that don't reference --output-format remain unchanged
    - All existing tests for per-page, per-sentence modes remain unchanged
  </behavior>
  <action>
    Read the full set of --output-format test functions (lines ~819–1070 area) to identify exact function names and line ranges. Delete each function that tests --output-format as a valid option. Delete any helper functions/fixtures that exist solely to support those tests (e.g., _run_monolingual_with_format helper if it exists). Add the three new test functions described in behavior. Follow the existing mock/patch pattern used by test_monolingual_* tests for the new test_interactive_mode_is_valid() test — mock both assemble_interactive and the translate call to avoid network calls.
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -m pytest tests/test_cli.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>test_cli.py passes fully; no test references --output-format as a valid accepted option; test_output_format_option_does_not_exist passes; test_interactive_mode_is_valid passes; test suite exit code 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user input → mode validation | User-supplied --mode value crosses validation boundary in translate_cmd(); VALID_MODES is the gate |
| CLI → assembler dispatch | effective_mode string routes to one of three assembler functions; wrong routing = wrong output format |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Elevation of Privilege | VALID_MODES gate | mitigate | "interactive" only added after translation pipeline is complete; VALID_MODES set membership check at line ~151 blocks unknown modes with exit code 2; no new privilege surface |
| T-12-04 | Tampering | assemble_interactive dispatch | accept | Interactive mode uses same translation pipeline as per-page (no sentence chunking); no new data handling; same atomic EPUB write pattern as assemble() |
| T-12-05 | Denial of Service | --output-format removal breaking existing scripts | accept | Users passing --output-format will receive Typer "no such option" exit code 2 — clear error, not silent corruption; accepted breakage per D-02 user decision |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
1. `python -m pytest tests/test_cli.py -x -q` — all tests pass; no failures
2. `python -m pytest tests/test_builder.py tests/test_assembler.py -x -q` — no regressions
3. `python -c "from book_translator.cli import VALID_MODES; assert VALID_MODES == {'per-page','per-sentence','monolingual','interactive'}; print('modes OK')"` — passes
4. `python -c "from book_translator.assembler import assemble, assemble_interactive, assemble_monolingual, __all__; assert 'assemble_interactive' in __all__; print('__all__ OK')"` — passes
5. `grep -c "FORMAT_TO_EXT\|VALID_OUTPUT_FORMATS\|output_format" src/book_translator/cli.py` — returns 0 (all dead code removed)
6. `python -m pytest tests/ -x -q` — full suite passes
</verification>

<success_criteria>
- assemble_interactive() in assembler/__init__.py; "assemble_interactive" in __all__
- VALID_MODES = {"per-page", "per-sentence", "monolingual", "interactive"} (D-01, INTR-03)
- VALID_OUTPUT_FORMATS, FORMAT_TO_EXT, output_format Typer option absent from cli.py (D-02)
- Dispatch branch calls assemble_interactive() for interactive mode (D-03)
- assemble_monolingual() called without output_format arg at cli.py call site (D-04)
- batch_token_budget validation unchanged (D-05)
- test_cli.py passes with --output-format tests removed and interactive mode test added
- Full test suite passes
</success_criteria>

## Artifacts this phase produces

| Symbol | Type | File | Description |
|--------|------|------|-------------|
| `assemble_interactive` | function | `src/book_translator/assembler/__init__.py` | Public assembler wrapper for interactive EPUB; identical structure to assemble() |
| `VALID_MODES` | updated constant | `src/book_translator/cli.py` | Now includes "interactive" alongside per-page, per-sentence, monolingual |
| `test_output_format_option_does_not_exist` | test function | `tests/test_cli.py` | Asserts --output-format is rejected as unknown option |
| `test_interactive_mode_is_valid` | test function | `tests/test_cli.py` | Asserts --mode interactive dispatches to assemble_interactive() |

<output>
Create `.planning/phases/12-css-cli-integration/12-02-SUMMARY.md` when done
</output>
