---
phase: 11-html-generation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/book_translator/assembler/html_gen.py
  - tests/test_assembler.py
autonomous: true
requirements:
  - INTR-02
  - INTR-06
  - INTR-07
  - INTR-08
  - INTR-09
  - INTR-11
  - INTR-12
  - INTR-18
  - INTR-19

must_haves:
  truths:
    - "build_interactive_html(para, 'ru') returns <details class='bt-interactive'> for paragraph/caption/footnote kinds"
    - "build_interactive_html with is_first=True returns <details ... open='open'>"
    - "build_interactive_html for heading kind returns <h2> with inline <span class='bt-heading-translation'> — no <details>"
    - "build_interactive_html for image/table kind returns para.raw_html unchanged"
    - "_prefix_ids is called on para.raw_html BEFORE <details> assembly; <details> never enters BS4"
    - "_XHTML_TEMPLATE starts with <!DOCTYPE html> (HTML5), not XHTML 1.1"
    - "All tests in tests/test_assembler.py pass"
  artifacts:
    - path: "src/book_translator/assembler/html_gen.py"
      provides: "build_interactive_html function + DOCTYPE fix"
      exports: ["build_interactive_html"]
      contains: "def build_interactive_html"
    - path: "tests/test_assembler.py"
      provides: "Tests for build_interactive_html covering all kinds + is_first flag"
      contains: "build_interactive_html"
  key_links:
    - from: "build_interactive_html"
      to: "_prefix_ids"
      via: "call before <details> string interpolation"
      pattern: "_prefix_ids\\(para\\.raw_html\\)"
    - from: "_XHTML_TEMPLATE"
      to: "HTML5 DOCTYPE"
      via: "first line of template string"
      pattern: "<!DOCTYPE html>"
---

<objective>
Fix the XHTML DOCTYPE bug and implement `build_interactive_html()` — the core rendering function for interactive EPUB mode.

Purpose: Establishes the rendering contract (all paragraph kinds → correct HTML structure) that `build_interactive()` in builder.py will call. Fixes DOCTYPE so `<details>`/`<summary>` are valid in generated HTML.

Output: Updated `html_gen.py` with DOCTYPE fix and new `build_interactive_html` function; expanded `tests/test_assembler.py` with coverage of all kinds and the `is_first` flag.
</objective>

<execution_context>
@/Users/aefimov/.claude/gsd-core/workflows/execute-plan.md
@/Users/aefimov/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-html-generation-engine/11-CONTEXT.md
@.planning/phases/11-html-generation-engine/11-RESEARCH.md
@.planning/phases/11-html-generation-engine/11-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix DOCTYPE and implement build_interactive_html in html_gen.py</name>
  <files>src/book_translator/assembler/html_gen.py</files>
  <read_first>
    - src/book_translator/assembler/html_gen.py — read full file before touching; note _XHTML_TEMPLATE lines 15-28, _PASS_THROUGH_KINDS line 12, _prefix_ids function lines 50-71, build_pair_html lines 74-113
    - .planning/REQUIREMENTS.md — exact HTML structures for INTR-06, INTR-09 (class names, xml:lang/lang attributes)
    - .planning/phases/11-html-generation-engine/11-CONTEXT.md — locked decisions D-03, D-07, D-08, D-09
  </read_first>
  <behavior>
    - Test 1 (INTR-02): `_XHTML_TEMPLATE` starts with `<!DOCTYPE html>\n` — the `<?xml ...?>` and XHTML 1.1 DOCTYPE lines are gone
    - Test 2 (INTR-19/INTR-11): `build_interactive_html(image_para, "ru")` returns `image_para.raw_html` unchanged
    - Test 3 (INTR-19/INTR-11): `build_interactive_html(table_para, "ru")` returns `table_para.raw_html` unchanged
    - Test 4 (INTR-06): `build_interactive_html(para, "ru")` for `kind="paragraph"` returns string containing `<details class="bt-interactive">`, `<summary class="bt-original">`, `<p class="bt-translation" xml:lang="ru" lang="ru">`
    - Test 5 (INTR-07): `build_interactive_html(para, "ru", is_first=True)` returns string containing `open="open"` on the `<details>` tag
    - Test 6 (INTR-07): `build_interactive_html(para, "ru", is_first=False)` (default) returns string where `<details>` does NOT contain `open=`
    - Test 7 (INTR-08): `build_interactive_html(caption_para, "ru")` for `kind="caption"` returns `<details class="bt-interactive">` structure (same as paragraph)
    - Test 8 (INTR-08): `build_interactive_html(footnote_para, "ru")` for `kind="footnote"` returns `<details class="bt-interactive">` structure
    - Test 9 (INTR-09): `build_interactive_html(heading_para, "ru")` for `kind="heading"` returns `<h2>` with `<span class="bt-heading-translation" xml:lang="ru" lang="ru">` — no `<details>` anywhere in output
    - Test 10 (INTR-18): When para.raw_html contains `id="foo"`, the `<summary>` inner content has the id prefixed (`id="bt-orig-foo"`) — confirming _prefix_ids ran before <details> assembly
    - Test 11 (INTR-12): For `kind="paragraph"`, output contains both summary content and translation `<p>` — both are present (no content loss)
  </behavior>
  <action>
    1. Fix `_XHTML_TEMPLATE`: replace the first two lines (`<?xml version="1.0"...?>` and the two-line XHTML 1.1 DOCTYPE) with `<!DOCTYPE html>`. Keep `<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="{lang}">` unchanged — ebooklib serialization requires the xmlns and xml:lang. (INTR-02)

    2. Add `import html as _html` at the top-level imports of `html_gen.py` (needed for heading text escaping in `build_interactive_html`).

    3. Add `build_interactive_html` as a new module-level function after `build_pair_html`. Do NOT modify `build_pair_html`. Signature per D-03: `build_interactive_html(para: Paragraph, target_lang: str, is_first: bool = False) -> str`.

    Implementation logic (per D-07, D-08, D-09, INTR-06 through INTR-12, INTR-18):
    - If `para.kind in _PASS_THROUGH_KINDS`: return `para.raw_html` (D-09, INTR-11)
    - If `para.kind == "heading"`: escape `para.text` with `_html.escape`, build `<span class="bt-heading-translation" xml:lang="{target_lang}" lang="{target_lang}">{para.translation or ""}</span>`, return `f"<h2>{escaped_text}{span}</h2>"` (INTR-09)
    - Otherwise (paragraph/caption/footnote): call `_prefix_ids(para.raw_html)` first (D-08, INTR-18), set `open_attr = ' open="open"'` if `is_first` else `""` (INTR-07), return `<details class="bt-interactive"{open_attr}><summary class="bt-original">{prefixed_orig}</summary><p class="bt-translation" xml:lang="{target_lang}" lang="{target_lang}">{para.translation or ""}</p></details>` (INTR-06, INTR-08)

    Do NOT call `_inject_class` inside `build_interactive_html` — class goes on `<summary>` and `<details>` directly via string interpolation, not on inner elements (per D-08).
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -m pytest tests/test_assembler.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `_XHTML_TEMPLATE` first line is `<!DOCTYPE html>` (not `<?xml ...?>` or XHTML DOCTYPE)
    - `build_interactive_html` exists in `html_gen.py` with signature `(para: Paragraph, target_lang: str, is_first: bool = False) -> str`
    - All 11 behavior tests pass
    - `build_pair_html` is unmodified
  </done>
</task>

<task type="auto">
  <name>Task 2: Add build_interactive_html tests to test_assembler.py</name>
  <files>tests/test_assembler.py</files>
  <read_first>
    - tests/test_assembler.py — read full file; note import block lines 1-12, Paragraph fixture patterns, existing assertion style (BS4 parse assertions vs string assertions)
    - src/book_translator/assembler/html_gen.py — after Task 1 completes; confirm `build_interactive_html` signature and `_XHTML_TEMPLATE` change
    - .planning/phases/11-html-generation-engine/11-PATTERNS.md — Test Patterns section (BS4 assertion pattern, fixture pattern)
  </read_first>
  <action>
    Add `build_interactive_html` to the html_gen import in the test file.

    Add a `TestBuildInteractiveHtml` test class covering all 11 behaviors from Task 1's behavior block. Use `BeautifulSoup(result, "lxml")` to parse and assert structure — do not rely on raw string equality for HTML attribute order.

    Key assertions:
    - `soup.find("details")["class"]` contains `"bt-interactive"` (INTR-06)
    - `soup.find("summary")["class"]` contains `"bt-original"` (INTR-06)
    - `soup.find("p")["class"]` contains `"bt-translation"` with correct `xml:lang` and `lang` (INTR-06)
    - `"open" in soup.find("details").attrs` is True when `is_first=True` (INTR-07)
    - `"open" not in soup.find("details").attrs` when `is_first=False` (INTR-07)
    - `soup.find("details")` is None for heading kind; `soup.find("h2")` is not None (INTR-09)
    - `soup.find("span")["class"]` contains `"bt-heading-translation"` for heading (INTR-09)
    - Pass-through: result equals `para.raw_html` exactly (INTR-11)
    - ID prefixing: `soup.find(id="bt-orig-foo")` exists when para.raw_html has `id="foo"` (INTR-18)

    Add a `TestDoctype` test that asserts `_XHTML_TEMPLATE.startswith("<!DOCTYPE html>")` (INTR-02).
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -m pytest tests/test_assembler.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All tests in test_assembler.py pass
    - `TestBuildInteractiveHtml` and `TestDoctype` classes visible in pytest -v output
    - No test regressions
  </done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Type | File | Notes |
|--------|------|------|-------|
| `build_interactive_html` | function | `src/book_translator/assembler/html_gen.py` | New; signature `(para: Paragraph, target_lang: str, is_first: bool = False) -> str` |
| `_XHTML_TEMPLATE` (modified) | module variable | `src/book_translator/assembler/html_gen.py` | DOCTYPE changed from XHTML 1.1 to `<!DOCTYPE html>` |
| `TestBuildInteractiveHtml` | test class | `tests/test_assembler.py` | New; 11+ test methods |
| `TestDoctype` | test class | `tests/test_assembler.py` | New; 1 test for DOCTYPE |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| para.raw_html → HTML output | Paragraph HTML from upstream parser enters build_interactive_html; output embedded in EPUB |
| para.translation → HTML output | Translation string from LLM enters output HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `para.translation` embedded in `<p class="bt-translation">` | accept | Translation content is LLM output already trusted by the pipeline; no user-controlled XSS surface in EPUB readers |
| T-11-02 | Tampering | `para.raw_html` embedded in `<summary>` | accept | Raw HTML originates from the parsed EPUB source, not from external user input; same trust level as existing `build_pair_html` |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- `python -m pytest tests/test_assembler.py -x -q` exits 0
- `grep -c "def build_interactive_html" src/book_translator/assembler/html_gen.py` outputs `1`
- `grep -c "<!DOCTYPE html>" src/book_translator/assembler/html_gen.py` outputs `1`
- `grep -c "XHTML 1.1" src/book_translator/assembler/html_gen.py` outputs `0`
- `grep -c "class TestBuildInteractiveHtml" tests/test_assembler.py` outputs `1`
</verification>

<success_criteria>
- `build_interactive_html` function present in `html_gen.py` with correct signature (D-03)
- DOCTYPE is `<!DOCTYPE html>` — no XHTML 1.1 DOCTYPE anywhere (INTR-02)
- paragraph/caption/footnote kinds produce `<details class="bt-interactive">` with `open="open"` when `is_first=True` (INTR-06, INTR-07, INTR-08)
- heading kind produces `<h2>` with `<span class="bt-heading-translation">` — no `<details>` (INTR-09)
- image/table kinds return `para.raw_html` unchanged (INTR-11)
- `_prefix_ids` called before `<details>` assembly — never passes `<details>` to BS4 (INTR-18)
- `build_pair_html` is unmodified (INTR-19)
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/11-html-generation-engine/11-01-SUMMARY.md` when done
</output>
