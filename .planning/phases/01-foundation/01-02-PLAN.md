---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/book_translator/models/document.py
  - src/book_translator/models/job.py
autonomous: true
requirements: [PARSE-01, PARSE-02, PARSE-03, JOB-03]

must_haves:
  truths:
    - "BookDocument round-trips through JSON without data loss"
    - "Paragraph stores original text, raw HTML, and optional translation slot"
    - "Chapter holds stable ID, optional title, and list of paragraphs"
    - "JobMeta stores only model name and params dict (no language pair, no book name)"
    - "translation field is str | None, not str, to distinguish untranslated from empty"
  artifacts:
    - path: "src/book_translator/models/document.py"
      provides: "Paragraph, Chapter, BookDocument Pydantic models"
      contains: "class BookDocument"
    - path: "src/book_translator/models/job.py"
      provides: "JobMeta dataclass"
      contains: "class JobMeta"
  key_links:
    - from: "BookDocument"
      to: "Paragraph"
      via: "Chapter.paragraphs list"
      pattern: "paragraphs: list\\[Paragraph\\]"
    - from: "BookDocument.from_json"
      to: "BookDocument.to_json"
      via: "model_validate_json / model_dump_json"
      pattern: "model_validate_json"
---

<objective>
Implement the core IR data models: `Paragraph`, `Chapter`, `BookDocument` as Pydantic v2 models in `document.py`, and `JobMeta` as a plain dataclass in `job.py`.

Purpose: These types are the shared contracts consumed by all subsequent phases (parsers, translator, assembler). Getting fields and types right here prevents breaking changes in every downstream phase.
Output: Two model files with correct field types, serialization methods, and docstrings.
</objective>

<execution_context>
@/Users/aefimov/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/aefimov/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Paragraph, Chapter, BookDocument models in document.py</name>
  <files>src/book_translator/models/document.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 1 (Pydantic v2 IR Models), Pitfall 3 (raw_html), Pitfall 4 (translation typing), Code Examples section (BookDocument round-trip)
    - src/book_translator/models/__init__.py (verify package exists before writing)
  </read_first>
  <action>
    Create `src/book_translator/models/document.py` with these exact classes:

    `Paragraph(BaseModel)` fields:
    - `id: str` — stable paragraph ID within chapter, e.g. `"ch01:p003"`
    - `text: str` — original plain text (used by AI for translation)
    - `raw_html: str` — original HTML markup verbatim (required for EPUB round-trip; for TXT/Markdown set equal to `text`)
    - `translation: str | None = None` — `None` means not yet translated; empty string means translation produced empty result
    - `kind: Literal["paragraph", "heading", "caption", "footnote"] = "paragraph"`

    `Chapter(BaseModel)` fields:
    - `id: str` — stable chapter ID (from EPUB spine item ID)
    - `title: str = ""`
    - `paragraphs: list[Paragraph] = Field(default_factory=list)`

    `BookDocument(BaseModel)` fields:
    - `title: str = ""`
    - `author: str = ""`
    - `source_lang: str = ""` — BCP-47 tag, e.g. `"ru"`
    - `chapters: list[Chapter] = Field(default_factory=list)`
    - `def to_json(self) -> str:` — returns `self.model_dump_json(indent=2)`
    - `@classmethod def from_json(cls, data: str) -> "BookDocument":` — returns `cls.model_validate_json(data)`

    Imports needed: `from __future__ import annotations`, `from typing import Literal`, `from pydantic import BaseModel, Field`.

    Use `str | None` syntax (not `Optional[str]`) — project targets Python 3.11+.
    Use `Field(default_factory=list)` for all list fields — never `default=[]`.
    Do NOT add `model_config` or custom validators in Phase 1 — keep models minimal.
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -c "
from book_translator.models.document import BookDocument, Chapter, Paragraph
p = Paragraph(id='ch01:p001', text='Hello', raw_html='<p>Hello</p>')
assert p.translation is None
c = Chapter(id='ch01', paragraphs=[p])
doc = BookDocument(title='T', source_lang='en', chapters=[c])
j = doc.to_json()
doc2 = BookDocument.from_json(j)
assert doc2.chapters[0].paragraphs[0].text == 'Hello'
assert doc2.chapters[0].paragraphs[0].raw_html == '<p>Hello</p>'
assert doc2.chapters[0].paragraphs[0].translation is None
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `src/book_translator/models/document.py` contains `class BookDocument(BaseModel)`
    - `src/book_translator/models/document.py` contains `class Chapter(BaseModel)`
    - `src/book_translator/models/document.py` contains `class Paragraph(BaseModel)`
    - `Paragraph.translation` field is typed `str | None` with default `None`
    - `Paragraph.raw_html` is a required `str` field (no default)
    - `BookDocument.from_json(doc.to_json())` round-trips without data loss (verified by automated command above)
    - `ruff check src/book_translator/models/document.py` exits 0
  </acceptance_criteria>
  <done>document.py exists; round-trip assertion passes; ruff clean</done>
</task>

<task type="auto">
  <name>Task 2: Implement JobMeta dataclass in job.py</name>
  <files>src/book_translator/models/job.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 2 (JobMeta definition), Pitfall 1 (no derived data in metadata), Assumptions Log A4
    - src/book_translator/models/document.py (just created — see import style conventions)
  </read_first>
  <action>
    Create `src/book_translator/models/job.py` with:

    `JobMeta` as a plain `@dataclass` (not Pydantic — per research Assumption A4: simpler for small struct):
    - `model: str` — AI model identifier, e.g. `"openai/gpt-4o"`
    - `params: dict` — arbitrary API params (temperature, max_tokens, etc.); opaque at this layer

    Do NOT add `lang_from`, `lang_to`, `book_name`, or any other field to `JobMeta` — those are derived from file names (per JOB-03 / Pitfall 1).

    Imports needed: `from __future__ import annotations`, `from dataclasses import dataclass, field`.

    Use `field(default_factory=dict)` for `params` so `JobMeta(model="gpt-4o")` is valid without specifying params.
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -c "
from book_translator.models.job import JobMeta
import dataclasses
m = JobMeta(model='openai/gpt-4o', params={'temperature': 0.3})
assert m.model == 'openai/gpt-4o'
assert m.params == {'temperature': 0.3}
m2 = JobMeta(model='openai/gpt-4o')
assert m2.params == {}
fields = {f.name for f in dataclasses.fields(m)}
assert fields == {'model', 'params'}, f'unexpected fields: {fields}'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `src/book_translator/models/job.py` contains `@dataclass` decorator on `JobMeta`
    - `JobMeta` has exactly two fields: `model: str` and `params: dict`
    - `JobMeta(model="x")` instantiates without error (params defaults to `{}`)
    - `JobMeta` does NOT have fields named `lang_from`, `lang_to`, or `book_name`
    - `ruff check src/book_translator/models/job.py` exits 0
  </acceptance_criteria>
  <done>job.py exists; dataclass field assertions pass; ruff clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase 1 has no network calls, no user input, no parsing of untrusted content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pip install (pydantic) | accept | pydantic is a long-established ecosystem foundation with >100M/mo downloads; Package Legitimacy Audit in RESEARCH.md confirms Approved disposition |
</threat_model>

<verification>
- `python -c "from book_translator.models.document import BookDocument, Chapter, Paragraph"` exits 0
- `python -c "from book_translator.models.job import JobMeta"` exits 0
- BookDocument round-trip test (see Task 1 verify command) passes
- JobMeta field count assertion (see Task 2 verify command) passes
- `ruff check src/book_translator/models/` exits 0
</verification>

<success_criteria>
`document.py` and `job.py` exist with correct Pydantic/dataclass definitions. `BookDocument.from_json(doc.to_json())` round-trips without data loss. `JobMeta` has exactly `model` and `params` fields. All ruff checks pass.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
