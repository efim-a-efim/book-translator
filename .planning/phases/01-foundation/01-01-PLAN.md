---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/book_translator/__init__.py
  - src/book_translator/models/__init__.py
  - src/book_translator/store/__init__.py
autonomous: true
requirements: [JOB-01, JOB-02, JOB-03, PARSE-01, PARSE-02, PARSE-03]

must_haves:
  truths:
    - "Package is importable after `pip install -e .`"
    - "ruff runs without errors on empty package"
    - "pytest discovers test directory"
    - "pyproject.toml declares all runtime dependencies and dev tooling"
  artifacts:
    - path: "pyproject.toml"
      provides: "Build system config, dependencies, scripts entry point, ruff + pytest config"
      contains: "requires-python"
    - path: "src/book_translator/__init__.py"
      provides: "Package root"
    - path: "src/book_translator/models/__init__.py"
      provides: "models sub-package"
    - path: "src/book_translator/store/__init__.py"
      provides: "store sub-package"
  key_links:
    - from: "pyproject.toml"
      to: "src/book_translator"
      via: "hatchling src layout"
      pattern: "tool.hatch.build.targets.wheel"
---

<objective>
Create the project scaffold: `pyproject.toml` with all dependencies, `src/` layout package directories, and dev tooling config (ruff, pytest).

Purpose: Every other plan depends on this scaffold existing. No source code imports can work without the package structure and `pip install -e .`.
Output: Installable Python package skeleton; ruff and pytest runnable.
</objective>

<execution_context>
@/Users/aefimov/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/aefimov/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml with full project metadata and tooling config</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 4 (pyproject.toml scaffold), Standard Stack section, Anti-Patterns section
  </read_first>
  <action>
    Create `pyproject.toml` in the project root with these exact sections:

    Build system: `requires = ["hatchling"]`, `build-backend = "hatchling.build"`.

    `[project]` fields: `name = "book-translator"`, `version = "0.1.0"`, `description = "AI-powered bilingual book translator"`, `readme = "README.md"`, `requires-python = ">=3.11"`.

    `[project.dependencies]`: `pydantic>=2.0`, `ebooklib>=0.18`, `beautifulsoup4>=4.12`, `lxml>=5.0`, `openai>=1.0`, `typer>=0.9`, `rich>=13.0`, `tenacity>=8.0`.

    `[project.scripts]`: `book-translator = "book_translator.cli:app"`.

    `[project.optional-dependencies]`: `dev = ["ruff", "pytest", "pytest-asyncio"]`.

    `[tool.hatch.build.targets.wheel]`: `packages = ["src/book_translator"]` — required for `src/` layout with hatchling.

    `[tool.ruff]`: `line-length = 100`, `target-version = "py311"`.

    `[tool.ruff.lint]`: `select = ["E", "F", "I", "UP"]`.

    `[tool.pytest.ini_options]`: `testpaths = ["tests"]`, `asyncio_mode = "auto"`.

    Do NOT create `setup.py` or `requirements.txt`.
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['requires-python']=='>=3.11'; assert 'pydantic>=2.0' in d['project']['dependencies']; assert 'book-translator' in d['project']['scripts']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `requires-python = ">=3.11"`
    - pyproject.toml contains `pydantic>=2.0` in `[project.dependencies]`
    - pyproject.toml contains `book-translator = "book_translator.cli:app"` in `[project.scripts]`
    - pyproject.toml contains `asyncio_mode = "auto"` in `[tool.pytest.ini_options]`
    - pyproject.toml contains `packages = ["src/book_translator"]` in `[tool.hatch.build.targets.wheel]`
    - No `setup.py` exists in project root
  </acceptance_criteria>
  <done>pyproject.toml present and all assertions above pass</done>
</task>

<task type="auto">
  <name>Task 2: Create src/ layout package directories and install in editable mode</name>
  <files>
    src/book_translator/__init__.py
    src/book_translator/models/__init__.py
    src/book_translator/store/__init__.py
    tests/__init__.py
    tests/conftest.py
  </files>
  <read_first>
    - pyproject.toml (just created — verify src layout config before creating dirs)
    - .planning/phases/01-foundation/01-RESEARCH.md — Recommended Project Structure section, Pitfall 6
  </read_first>
  <action>
    Create the following directory structure under project root:

    `src/book_translator/__init__.py` — empty file (or `__version__ = "0.1.0"`).
    `src/book_translator/models/__init__.py` — empty file.
    `src/book_translator/store/__init__.py` — empty file.
    `tests/__init__.py` — empty file.
    `tests/conftest.py` — empty file (placeholder; fixtures added in Plan 03).

    After creating files, run `pip install -e ".[dev]"` to install the package in editable mode so all subsequent plans can import `book_translator`.

    Do NOT create `src/book_translator/cli.py` — that belongs to Phase 5.
    Do NOT create `src/book_translator/models/document.py` or `job.py` — those belong to Plan 02.
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -c "import book_translator; print('import OK')" && python -m pytest --collect-only -q 2>&1 | grep -v "no tests ran" | head -5 && ruff check src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import book_translator"` exits 0
    - `python -c "import book_translator.models"` exits 0
    - `python -c "import book_translator.store"` exits 0
    - `ruff check src/ tests/` exits 0 (no lint errors on empty files)
    - `python -m pytest --collect-only` exits 0 (no collection errors)
  </acceptance_criteria>
  <done>Package importable; ruff and pytest both exit 0 on the empty scaffold</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase 1 has no network calls, no user input, no parsing of untrusted content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pip install (pydantic, ruff, pytest, pytest-asyncio) | accept | All packages are long-established ecosystem foundations with public GitHub repos and >50M/mo downloads; Package Legitimacy Audit in RESEARCH.md confirms Approved disposition for all four |
</threat_model>

<verification>
- `python -c "import book_translator; import book_translator.models; import book_translator.store"` exits 0
- `ruff check src/ tests/` exits 0
- `python -m pytest --collect-only` exits 0
- `pyproject.toml` validated by `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"`
</verification>

<success_criteria>
Installable Python package scaffold exists. `book_translator`, `book_translator.models`, and `book_translator.store` are importable. ruff and pytest run without errors. pyproject.toml contains all runtime deps, dev tooling config, entry point declaration, and `src/` layout config.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
