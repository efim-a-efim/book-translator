---
phase: 07-mode-selection-cli-dispatch
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/book_translator/cli.py
  - tests/test_cli.py
autonomous: true
requirements:
  - MODE-01
  - MODE-02
must_haves:
  truths:
    - "D-05: Omitting --mode and passing --mode per-page use equivalent parser, translator, assembler, output, and cleanup behavior."
    - "The per-page path remains `_parse_file -> translate -> assemble` with the existing translator call signature."
    - "D-07: Per-page run metadata may include additive mode fields but never includes API keys or future-mode-only options."
    - "D-06: Phase 7 proves per-page compatibility through mocked dispatch equivalence; byte-identical EPUB proof remains Phase 10 scope."
  artifacts:
    - path: "src/book_translator/cli.py"
      provides: "Effective per-page dispatch and safe additive mode metadata"
      contains: "mode"
    - path: "tests/test_cli.py"
      provides: "Mocked dispatch-equivalence and metadata secrecy regression tests"
      contains: "per_page"
  key_links:
    - from: "src/book_translator/cli.py"
      to: "book_translator.translator.translate"
      via: "unchanged per-page kwargs"
      pattern: "translate\("
    - from: "src/book_translator/cli.py"
      to: "meta.json"
      via: "JobMeta.params whitelist"
      pattern: "JobMeta"
---

<objective>
Prove and preserve per-page compatibility for omitted mode and explicit `--mode per-page`, while recording only safe additive mode metadata.

Purpose: The v2 CLI must not disturb the v1 per-page execution path, and any new metadata must remain non-secret and additive.
Output: Mocked dispatch-equivalence tests, safe metadata assertions, and minimal per-page metadata implementation if not already present.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-mode-selection-cli-dispatch/07-CONTEXT.md
@.planning/phases/07-mode-selection-cli-dispatch/07-RESEARCH.md
@.planning/phases/07-mode-selection-cli-dispatch/07-PATTERNS.md
@.planning/phases/07-mode-selection-cli-dispatch/07-01-SUMMARY.md
@src/book_translator/cli.py
@tests/test_cli.py

<interfaces>
Per-page dispatch contract that must remain unchanged except additive metadata:
- `_parse_file(input_file)` reads the original input file and returns a document with `to_json()` and `chapters`.
- `translate(job_dir=run_dir, model=model, api_key=resolved_api_key, base_url=resolved_base_url, source_lang=source_lang, target_lang=target_lang, context_window=context_window, concurrency=concurrency, max_retries=max_retries, progress_callback=progress_callback)` is the existing per-page translator call.
- `assemble(job_dir=run_dir, target_lang=target_lang)` is the existing EPUB assembly call.
- `JobMeta.params` is manually whitelisted and currently excludes API key values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add per-page dispatch-equivalence tests</name>
  <files>tests/test_cli.py</files>
  <read_first>
    @.planning/phases/07-mode-selection-cli-dispatch/07-CONTEXT.md
    @.planning/phases/07-mode-selection-cli-dispatch/07-RESEARCH.md
    @.planning/phases/07-mode-selection-cli-dispatch/07-PATTERNS.md
    @.planning/phases/07-mode-selection-cli-dispatch/07-01-SUMMARY.md
    @tests/test_cli.py
    @src/book_translator/cli.py
  </read_first>
  <behavior>
    - Omitted mode and explicit `--mode per-page` both exit 0 when `_parse_file`, `translate`, and `assemble` are mocked per D-05 and MODE-02.
    - Captured `_parse_file`, `translate`, and `assemble` calls are equivalent between omitted mode and explicit `per-page` per D-06.
    - Captured `translate(...)` kwargs do not include `mode`, `output_format`, or `batch_token_budget`, preserving the existing translator API.
    - Successful per-page runs still auto-delete the run directory after output copy.
  </behavior>
  <action>Add black-box CLI tests that run the same sample input twice: once with no `--mode`, once with `--mode per-page`. Use the existing mocked happy-path pattern with a fake document, fake async `translate`, and fake `assemble`. Capture parse arguments, translate kwargs excluding per-run `job_dir` identity when comparing shape, assemble target language, output bytes, and final `tmp_store` cleanup. Do not assert byte-identical EPUB content in this phase; D-06 assigns that proof to Phase 10.</action>
  <verify>
    <automated>./venv/bin/pytest tests/test_cli.py -q -k "per_page or mode"</automated>
  </verify>
  <acceptance_criteria>
    - The test compares omitted mode and explicit per-page through mocks rather than real API calls or EPUB baselines.
    - The test asserts the existing `_parse_file -> translate -> assemble` chain is used in both cases.
    - The test asserts no new future-mode-only values are passed into `translate(...)`.
    - The test does not require Phase 10 byte-identical EPUB fixtures.
  </acceptance_criteria>
  <done>Per-page compatibility is proven at the dispatch level required by Phase 7.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add safe mode metadata for per-page runs</name>
  <files>src/book_translator/cli.py, tests/test_cli.py</files>
  <read_first>
    @.planning/phases/07-mode-selection-cli-dispatch/07-CONTEXT.md
    @.planning/phases/07-mode-selection-cli-dispatch/07-RESEARCH.md
    @.planning/phases/07-mode-selection-cli-dispatch/07-PATTERNS.md
    @.planning/phases/07-mode-selection-cli-dispatch/07-01-SUMMARY.md
    @src/book_translator/cli.py
    @tests/test_cli.py
  </read_first>
  <action>Extend the existing manual `JobMeta.params` construction with additive, non-secret mode metadata per D-07: record `mode` as the effective mode value and `mode_explicit` as whether the user supplied `--mode`. Add or update tests that force a retained per-page run after run creation, such as a patched `ParseError`, then inspect `meta.json` for `params.mode == "per-page"`, `params.mode_explicit` matching omitted versus explicit mode, and absence of the provided API key. Do not serialize `api_key`, raw environment variables, `output_format`, or `batch_token_budget` into per-page metadata. Do not change `JobMeta`, `JobStore`, parser, translator, or assembler public APIs.</action>
  <verify>
    <automated>./venv/bin/pytest tests/test_cli.py -q -k "meta or mode or per_page"</automated>
  </verify>
  <acceptance_criteria>
    - Retained per-page run metadata contains only additive non-secret mode fields: `mode` and `mode_explicit`.
    - Existing `test_translate_no_api_key_in_meta_json` still passes.
    - `translate(...)` signature and kwargs remain unchanged from the v1 per-page path.
    - No per-sentence chunking, monolingual output writing, or Phase 10 byte-baseline logic is implemented.
  </acceptance_criteria>
  <done>Per-page mode metadata is present, explicitness is observable, and secret leakage tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> per-page dispatcher | User-provided mode choice controls the branch that reaches the existing v1 pipeline. |
| CLI/runtime options -> `JobMeta.params` | Selected non-secret values are persisted to `meta.json`. |
| Per-page dispatch -> translator engine | Existing public translator function receives kwargs from CLI orchestration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure | `JobMeta.params` / `meta.json` | mitigate | Continue whitelisting metadata fields; add only `mode` and `mode_explicit`; assert API key and future-mode-only options are absent. |
| T-07-05 | Tampering | Per-page translator invocation | mitigate | Mock and compare omitted-mode and explicit-per-page dispatch so new mode support cannot alter parser/translator/assembler behavior. |
| T-07-06 | Spoofing | Mode metadata | mitigate | Persist effective mode as `per-page` for omitted and explicit per-page runs, plus explicitness, so metadata does not misrepresent the actual pipeline. |
| T-07-SC | Tampering | Package installs | accept | Phase 7 installs no npm, pip, or cargo packages; use existing project dependencies only. |
</threat_model>

<verification>
Run these checks after completing the plan:
- `./venv/bin/pytest tests/test_cli.py -q -k "per_page or mode or meta"`
- `./venv/bin/pytest tests/test_cli.py -q`
- `./venv/bin/ruff check src/book_translator/cli.py tests/test_cli.py`
</verification>

<success_criteria>
MODE-01 and MODE-02 are covered by mocked dispatch-equivalence tests. Omitted mode remains effective per-page. Explicit per-page and omitted mode use equivalent parser, translator, assembler, output, and cleanup behavior. Metadata is additive and non-secret.
</success_criteria>

<output>
Create `.planning/phases/07-mode-selection-cli-dispatch/07-02-SUMMARY.md` when done.
</output>
