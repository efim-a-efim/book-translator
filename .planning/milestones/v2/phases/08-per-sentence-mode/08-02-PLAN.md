---
phase: 08-per-sentence-mode
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/book_translator/translator/engine.py
  - src/book_translator/translator/prompt.py
  - tests/test_translator.py
autonomous: true
requirements:
  - SENT-06
  - SENT-07
  - SENT-08
  - SENT-09
  - SENT-10
must_haves:
  truths:
    - "SENT-06: Per-sentence EPUB renders each chunk's original followed by translation"
    - "SENT-07: Multiple chunks packed per request up to batch-token-budget"
    - "SENT-08: Token budget configurable via --batch-token-budget (default 4000)"
    - "SENT-09: Each request uses structured output with per-chunk IDs"
    - "SENT-10: Failed batches retry per policy; persistent failure retains run"
  artifacts:
    - path: "src/book_translator/translator/engine.py"
      provides: "Per-sentence translation dispatch and batch processing"
      contains: "translate_sentence"
    - path: "tests/test_translator.py"
      provides: "Per-sentence translation and batch tests"
      contains: "per_sentence"
---

<objective>
Implement per-sentence translation with batched structured-output AI requests.

Purpose: Connect sentence chunking to the translation engine with token-budget batching and structured output.
Output: Per-sentence translate function, batch building with token budget, and integration tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add per-sentence batch building</name>
  <files>src/book_translator/translator/chunker.py</files>
  <read_first>
    @.planning/phases/08-per-sentence-mode/08-CONTEXT.md
    @.planning/phases/08-per-sentence-mode/08-01-SUMMARY.md
    @src/book_translator/translator/chunker.py
  </read_first>
  <behavior>
    - build_sentence_batches(chunks, token_budget) groups SentenceChunks into batches
    - Each batch respects the token budget limit
    - Batches include context for translation continuity
  </action>
  <verify>
    <automated>./venv/bin/pytest tests/test_translator.py -q -k "sentence_batch"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-sentence translation function</name>
  <files>src/book_translator/translator/engine.py</files>
  <read_first>
    @.planning/phases/08-per-sentence-mode/08-CONTEXT.md
    @.planning/phases/08-per-sentence-mode/08-01-SUMMARY.md
    @src/book_translator/translator/engine.py
  </read_first>
  <behavior>
    - translate_sentence() handles sentence-level translation
    - Uses existing retry policy for failed batches
    - Returns chunk-ID-keyed translations
    - Integrates with existing translate() entry point
  </action>
  <verify>
    <automated>./venv/bin/pytest tests/test_translator.py -q -k "per_sentence"