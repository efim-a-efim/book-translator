---
phase: "10.1"
plan: 01
subsystem: translator/assembler
tags: [sent-06, sentence-rendering, data-model, html-gen]
dependency_graph:
  requires: []
  provides: [Paragraph.sentence_chunk_texts, build_pair_html-chunk-texts-path]
  affects: [html_gen.py, engine.py, document.py]
tech_stack:
  added: []
  patterns: [pydantic-field, min-mismatch-rule, fallback-compat]
key_files:
  created: []
  modified:
    - src/book_translator/models/document.py
    - src/book_translator/translator/engine.py
    - src/book_translator/assembler/html_gen.py
    - tests/test_translator.py
    - tests/test_assembler.py
decisions:
  - "Keep _split_sentences_for_rendering() as private fallback for backwards compat with old job JSON that lacks sentence_chunk_texts"
  - "Use min() rule to drop extras silently; removed [TRANSLATION FAILED] from per-sentence path"
  - "Updated existing test_build_pair_html_sentence_translations_partial to expect new min() behavior (Rule 1 - Bug)"
metrics:
  duration: "~10 minutes"
  completed: "2026-06-11"
  tasks: 3
  files: 5
---

# Phase 10.1 Plan 01: Fix SENT-06 — Align Sentence Rendering with Chunk-Based Translations Summary

**One-liner:** Carry chunk.text alongside translations through Paragraph model so build_pair_html() renders exact chunker segmentation instead of regex-resplit.

## What Was Done

### Task 1: Add sentence_chunk_texts to Paragraph and populate in translate_sentence()

Added `sentence_chunk_texts: list[str] | None = None` to `Paragraph` model immediately after `sentence_translations`. In `translate_sentence()` inner loop, appended `chunk.text` to `para.sentence_chunk_texts` in lock-step with `para.sentence_translations.append(translation)`.

**Commit:** 77fe09d

Files modified:
- `src/book_translator/models/document.py` — new field on line 16
- `src/book_translator/translator/engine.py` — populate in inner loop (lines 304-306)
- `tests/test_translator.py` — 3 new tests

### Task 2: Fix build_pair_html() and assembler tests

Replaced the per-sentence branch in `build_pair_html()` to use `sentence_chunk_texts` as primary source when set, falling back to `_split_sentences_for_rendering(para.text)` for backwards compat. Applied `min(len(source_texts), len(translations))` rule — no more `[TRANSLATION FAILED]` entries in per-sentence path. Kept `_split_sentences_for_rendering()` as private helper for the fallback branch.

**Commit:** 457d81d

Files modified:
- `src/book_translator/assembler/html_gen.py` — updated build_pair_html() per-sentence branch
- `tests/test_assembler.py` — 3 new tests + 1 existing test updated

### Task 3: Full suite verification

All 182 tests pass.

## Verification Results

```
PYTHONPATH=<worktree>/src python -m pytest --tb=short -q
182 passed in 15.69s

grep sentence_chunk_texts src/book_translator/models/document.py   -> line 16
grep sentence_chunk_texts src/book_translator/translator/engine.py -> lines 304-306
grep sentence_chunk_texts src/book_translator/assembler/html_gen.py -> lines 91-93
```

## Requirements Satisfied

- SENT-06: per-sentence EPUB now renders each chunk's original text (as produced by build_sentence_chunks) immediately followed by its translation — no mismatched pairing

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated existing test expecting [TRANSLATION FAILED] in per-sentence fallback path**
- **Found during:** Task 2 implementation
- **Issue:** `test_build_pair_html_sentence_translations_partial` asserted `[TRANSLATION FAILED]` in result, but the plan's must_haves require removing that and using min() rule instead
- **Fix:** Updated assertion to check `[TRANSLATION FAILED]" not in result` — consistent with new min() drop behavior
- **Files modified:** `tests/test_assembler.py`
- **Commit:** 457d81d

**2. [Rule 3 - Blocking] Tests required PYTHONPATH override due to editable install pointing to main checkout**
- **Found during:** Task 1 GREEN phase
- **Issue:** The venv's editable install uses sys.path entry for the main checkout's src/, not the worktree's src/. Without PYTHONPATH override, Python imports the unmodified main checkout files.
- **Fix:** All test runs used PYTHONPATH=<worktree>/src python -m pytest
- **Impact:** No code changes needed; purely execution environment issue

## Known Stubs

None.

## Threat Flags

None — changes add a Pydantic field (no new network endpoints or auth paths) and modify internal HTML rendering logic. The min() rule mitigates T-10.1-02 (Tampering via mismatch lengths) as required.

## Self-Check: PASSED

- [x] src/book_translator/models/document.py — sentence_chunk_texts field present
- [x] src/book_translator/translator/engine.py — populate logic present
- [x] src/book_translator/assembler/html_gen.py — read logic present
- [x] Commits 77fe09d and 457d81d exist in git log
- [x] 182 tests pass, 0 failures
