---
phase: 11-html-generation-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/book_translator/assembler/builder.py
  - tests/test_builder.py
autonomous: true
requirements:
  - INTR-01
  - INTR-07
  - INTR-10

must_haves:
  truths:
    - "build(), build_monolingual(), and build_interactive() each create a css_item via _make_css_item() and call book.add_item(css_item) + ch_item.add_item(css_item) per chapter"
    - "build_interactive() exists on EpubBuilder and returns an epub.EpubBook"
    - "build_interactive() produces <details open='open'> on the first paragraph element of each chapter"
    - "build_interactive() renders chapter title with <span class='bt-heading-translation'> when a matching heading paragraph with translation exists"
    - "build_interactive() renders chapter title as plain <h1> when no matching heading paragraph is found"
    - "_make_css_item() helper exists as a module-level function in builder.py"
  artifacts:
    - path: "src/book_translator/assembler/builder.py"
      provides: "_make_css_item helper, build_interactive method, CSS plumbing in all three builders"
      exports: ["EpubBuilder", "_make_css_item"]
      contains: "def build_interactive"
    - path: "tests/test_builder.py"
      provides: "Tests for CSS plumbing and build_interactive"
      contains: "build_interactive"
  key_links:
    - from: "build_interactive"
      to: "build_interactive_html"
      via: "import from html_gen, called per paragraph"
      pattern: "build_interactive_html\\("
    - from: "build_interactive"
      to: "_make_css_item"
      via: "called once per build call; returned item passed to book.add_item and ch_item.add_item"
      pattern: "_make_css_item\\("
    - from: "build / build_monolingual"
      to: "_make_css_item"
      via: "CSS plumbing added to existing builders (INTR-01)"
      pattern: "css_item = _make_css_item"
---

<objective>
Add CSS plumbing to all three builders (INTR-01) and implement `build_interactive()` with first-details tracking (INTR-07) and h1 span lookup (INTR-10).

Purpose: Wires the rendering function from Plan 01 into a complete EPUB assembly pipeline. Fixes the CSS packaging bug so stylesheets are actually applied. Phase 12 will supply the CSS content; Phase 11 establishes the plumbing with a stub.

Output: Updated `builder.py` with `_make_css_item`, CSS plumbing in `build`/`build_monolingual`/`build_interactive`, and the `build_interactive` method; tests validating CSS inclusion and interactive output structure.
</objective>

<execution_context>
@/Users/aefimov/.claude/gsd-core/workflows/execute-plan.md
@/Users/aefimov/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-html-generation-engine/11-CONTEXT.md
@.planning/phases/11-html-generation-engine/11-RESEARCH.md
@.planning/phases/11-html-generation-engine/11-PATTERNS.md
@.planning/phases/11-html-generation-engine/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _make_css_item, CSS plumbing, build_interactive to builder.py</name>
  <files>src/book_translator/assembler/builder.py</files>
  <read_first>
    - src/book_translator/assembler/builder.py — read full file; note build() lines 14-73, build_monolingual() lines 75-149, imports lines 1-10, TOC/spine wiring lines 47-71
    - .planning/phases/11-html-generation-engine/11-CONTEXT.md — D-01 (h1 lookup), D-02 (graceful fallback), D-04 (first-details tracking), D-05 (stub CSS content=b""), D-06 (_make_css_item signature)
    - .planning/phases/11-html-generation-engine/11-RESEARCH.md — Pattern 2 (CSS plumbing), Pattern 4 (first-details tracking), Pattern 5 (h1 span lookup)
    - .planning/phases/11-html-generation-engine/11-PATTERNS.md — builder.py pattern assignments; _make_css_item placement and _find_title_translation placement
    - src/book_translator/assembler/html_gen.py — confirm build_interactive_html is importable (from Plan 01)
  </read_first>
  <behavior>
    - Test 1 (INTR-01 / D-05): `_make_css_item()` returns an `epub.EpubItem` with `uid="style"`, `file_name="Styles/style.css"`, `media_type="text/css"`, `content=b""`
    - Test 2 (INTR-01): `_make_css_item(b"body{}")` returns item with `content=b"body{}"`
    - Test 3 (INTR-01): After `EpubBuilder().build(doc, "ru")`, the returned book's items include an item with `file_name="Styles/style.css"`
    - Test 4 (INTR-01): After `EpubBuilder().build_monolingual(doc, "ru")`, the returned book's items include an item with `file_name="Styles/style.css"`
    - Test 5 (INTR-01): After `EpubBuilder().build_interactive(doc, "ru")`, the returned book's items include an item with `file_name="Styles/style.css"`
    - Test 6 (INTR-07): In the EPUB returned by `build_interactive()`, the first chapter's first chapter item HTML contains `open="open"` exactly once (on the first `<details>`)
    - Test 7 (INTR-07): The second and subsequent `<details>` elements in a chapter do NOT have `open="open"`
    - Test 8 (INTR-10 / D-01): When `chapter.title == "Intro"` and a heading paragraph exists with `kind="heading"`, `text="Intro"`, `translation="Введение"`, the chapter h1 HTML contains `<span class="bt-heading-translation"` with the translation text
    - Test 9 (INTR-10 / D-02): When no heading paragraph matches `chapter.title`, the h1 HTML is `<h1>...</h1>` with no `<span>` element
    - Test 10: `EpubBuilder().build_interactive(doc, "ru")` returns an `epub.EpubBook` with spine containing chapter items and `"nav"`
  </behavior>
  <action>
    1. Add `from book_translator.models.document import BookDocument, Chapter` to imports (need `Chapter` type for `_find_title_translation`).

    2. Add `build_interactive_html` to the import from `book_translator.assembler.html_gen`.

    3. Add `_PASS_THROUGH_KINDS` import from `book_translator.assembler.html_gen` (needed for first-details tracking in `build_interactive`).

    4. Add `_make_css_item` as a module-level private function before the `EpubBuilder` class (per RESEARCH.md open question resolution — builder.py, not html_gen.py):
       Signature: `def _make_css_item(content: bytes = b"") -> epub.EpubItem`
       Returns `epub.EpubItem(uid="style", file_name="Styles/style.css", media_type="text/css", content=content)` (D-06)

    5. Add `_find_title_translation` as a module-level private function before `EpubBuilder`:
       Signature: `def _find_title_translation(chapter: Chapter, target_lang: str) -> str`
       Logic per D-01/D-02: search `chapter.paragraphs` for `p.kind == "heading" and p.text == chapter.title and p.translation`. If found: return `f'<h1>{_html.escape(chapter.title)}<span class="bt-heading-translation" xml:lang="{target_lang}" lang="{target_lang}">{match.translation}</span></h1>'`. If not found (or chapter.title is empty/None): return `f"<h1>{_html.escape(chapter.title)}</h1>"` (or `""` when no title). IMPORTANT per RESEARCH.md Pitfall 4: filter requires `p.translation` is truthy in the generator, not `p.translation is not None`.

    6. Add CSS plumbing to `build()` and `build_monolingual()` (INTR-01 / D-05):
       - After `book.add_item(epub.EpubNav())`, add: `css_item = _make_css_item()`; `book.add_item(css_item)`
       - Inside the per-chapter inner loop (after `ch_item.content = xhtml_content.encode("utf-8")`), add: `ch_item.add_item(css_item)`
       - In `build_monolingual()`, `book.add_item(ch_item)` already follows — add `ch_item.add_item(css_item)` before it

    7. Add `build_interactive` method to `EpubBuilder` (new method, after `build_monolingual`):
       Signature: `def build_interactive(self, doc: BookDocument, target_lang: str, book_id: str = "") -> epub.EpubBook`
       Structure mirrors `build_monolingual` with these differences:
       - After `book.add_item(epub.EpubNav())`, create `css_item = _make_css_item()` and `book.add_item(css_item)`
       - Chapter title: use `_find_title_translation(chapter, target_lang)` instead of plain h1
       - Per-paragraph loop: track `first_details_emitted = False` per chapter (reset each iteration). For each para, set `is_first = False`. If `para.kind not in _PASS_THROUGH_KINDS and para.kind != "heading"` and `not first_details_emitted`: set `is_first = True`, `first_details_emitted = True`. Append `build_interactive_html(para, target_lang, is_first=is_first)` to `html_parts`.
       - After `ch_item.content = xhtml_content.encode("utf-8")`, add `ch_item.add_item(css_item)` before `book.add_item(ch_item)`
       - TOC and spine wiring: identical to `build()` / `build_monolingual()` — copy verbatim
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -m pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `_make_css_item` function present before `EpubBuilder` class in `builder.py`
    - `_find_title_translation` function present before `EpubBuilder` class
    - `build_interactive` method present on `EpubBuilder`
    - `build()` and `build_monolingual()` both call `_make_css_item()` and `ch_item.add_item(css_item)`
    - All 10 behavior tests pass
    - All pre-existing tests pass (no regressions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add builder integration tests to test_builder.py</name>
  <files>tests/test_builder.py</files>
  <read_first>
    - tests/test_builder.py — read full file; note existing fixture patterns, how BookDocument/Chapter/Paragraph test objects are constructed, existing assertion style
    - src/book_translator/assembler/builder.py — after Task 1 completes; confirm _make_css_item, build_interactive signatures
    - .planning/phases/11-html-generation-engine/11-PATTERNS.md — Test Patterns section
  </read_first>
  <action>
    Add a `TestCssPlumbing` test class with tests verifying:
    - `build()` result contains a CSS item (`file_name="Styles/style.css"`) in `book.get_items()`
    - `build_monolingual()` result contains same CSS item
    - `build_interactive()` result contains same CSS item

    Add a `TestBuildInteractive` test class with tests verifying:
    - Returns `epub.EpubBook` instance
    - First chapter item content (decoded from UTF-8) contains `open="open"` (INTR-07)
    - Second paragraph in same chapter does NOT produce `open="open"` (INTR-07)
    - With a matching heading paragraph, chapter content contains `bt-heading-translation` span (INTR-10 / D-01)
    - Without matching heading paragraph, chapter content does NOT contain `bt-heading-translation` (INTR-10 / D-02)

    For fixtures: construct a minimal `BookDocument` with one `Chapter` containing 2–3 `Paragraph` objects of kind `"paragraph"` and optionally `"heading"`. Use `translation="Перевод"` on each paragraph. Pass `book_id="test-id"` to avoid UUID randomness.

    Extract chapter item content via: `items = [i for i in book.get_items() if hasattr(i, 'file_name') and i.file_name.endswith('.xhtml')]`; decode `item.content` to string for assertions.
  </action>
  <acceptance_criteria>
    - `TestCssPlumbing` class present in `tests/test_builder.py`
    - `TestBuildInteractive` class present with at least 5 test methods
    - `cd /Users/aefimov/ws/personal/book-translator && python -m pytest tests/test_builder.py -x -q` exits 0
    - `cd /Users/aefimov/ws/personal/book-translator && python -m pytest tests/ -x -q` exits 0 (full suite)
    - All previously passing tests still pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && python -m pytest tests/ -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Full test suite passes
    - `TestCssPlumbing` and `TestBuildInteractive` visible in pytest -v output
    - CSS plumbing and interactive builder covered by automated tests
  </done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Type | File | Notes |
|--------|------|------|-------|
| `_make_css_item` | function | `src/book_translator/assembler/builder.py` | Module-level; `(content: bytes = b"") -> epub.EpubItem`; uid="style", file_name="Styles/style.css" |
| `_find_title_translation` | function | `src/book_translator/assembler/builder.py` | Module-level; `(chapter: Chapter, target_lang: str) -> str`; implements D-01/D-02 |
| `EpubBuilder.build_interactive` | method | `src/book_translator/assembler/builder.py` | New method on existing class; `(self, doc: BookDocument, target_lang: str, book_id: str = "") -> epub.EpubBook` |
| `EpubBuilder.build` (modified) | method | `src/book_translator/assembler/builder.py` | CSS plumbing added: `_make_css_item()` + `book.add_item` + `ch_item.add_item` |
| `EpubBuilder.build_monolingual` (modified) | method | `src/book_translator/assembler/builder.py` | CSS plumbing added: same as build() |
| `TestCssPlumbing` | test class | `tests/test_builder.py` | New |
| `TestBuildInteractive` | test class | `tests/test_builder.py` | New; 5+ test methods |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chapter.title → HTML output | Chapter title string embedded in h1; originates from parsed EPUB metadata |
| para.translation → EPUB HTML content | LLM output embedded in chapter HTML items |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | `chapter.title` embedded in h1 via `_html.escape` | mitigate | `_html.escape()` applied to chapter.title before embedding — prevents HTML injection from adversarial EPUB metadata |
| T-11-04 | Tampering | `match.translation` embedded in span without escaping | accept | Translation is LLM output already trusted; same risk profile as existing build() translat insertion |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- `python -m pytest tests/ -x -q` exits 0
- `grep -c "def _make_css_item" src/book_translator/assembler/builder.py` outputs `1`
- `grep -c "def _find_title_translation" src/book_translator/assembler/builder.py` outputs `1`
- `grep -c "def build_interactive" src/book_translator/assembler/builder.py` outputs `1`
- `grep -c "css_item = _make_css_item" src/book_translator/assembler/builder.py` outputs `3` (one per builder)
- `grep -c "ch_item.add_item(css_item)" src/book_translator/assembler/builder.py` outputs at least `3`
</verification>

<success_criteria>
- `_make_css_item()` present as module-level function with correct EpubItem params (D-06)
- `_find_title_translation()` present implementing D-01/D-02 lookup logic
- `build_interactive()` present on EpubBuilder, returns epub.EpubBook
- All three builders (build, build_monolingual, build_interactive) call `_make_css_item()`, `book.add_item(css_item)`, and `ch_item.add_item(css_item)` (INTR-01 / D-05)
- First-details tracking per chapter emits `open="open"` on first paragraph-kind element only (INTR-07 / D-04)
- h1 title includes bt-heading-translation span when matching heading para with translation exists (INTR-10 / D-01)
- h1 title is plain when no match (INTR-10 / D-02)
- Full test suite passes
</success_criteria>

<output>
Create `.planning/phases/11-html-generation-engine/11-02-SUMMARY.md` when done
</output>
