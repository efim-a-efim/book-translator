---
phase: quick-260616-vb3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cli.py
  - tests/test_ephemeral.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Full test suite passes at 205 (210 baseline minus 5 removed), 0 collection/import errors"
    - "The 5 confirmed-extraneous test functions no longer exist in the suite"
    - "The 'no subcommands' guarantee remains covered by test_help_has_no_commands_section (CLI-05)"
    - "Live granularity validation remains covered by test_granularity_interactive_now_rejected"
    - "No KEPT test breaks as a side effect of a deletion (no kept test depended on a removed helper)"
  artifacts:
    - path: tests/test_ephemeral.py
      provides: "Run-dir lifecycle + CLI-05 surface coverage, minus the two transitional list/cleanup absence tests"
    - path: tests/test_cli.py
      provides: "CLI surface coverage, minus two exact-redundant happy-path tests and one removed-option absence guard"
  key_links:
    - from: tests/test_ephemeral.py::test_success_deletes_run_dir_and_writes_output
      to: "cli main() success path with --output"
      via: "runner.invoke + assemble side_effect"
      pattern: "out.read_bytes == b fake-epub"
    - from: tests/test_ephemeral.py::test_help_has_no_commands_section
      to: "single-command app surface (no subcommands)"
      via: "runner.invoke(app, --help)"
      pattern: "Commands not in result.output"
---

<objective>
Prune genuinely extraneous tests identified by deep analysis and confirmed with
the user. Phase 13 ("single-command-ephemeral-cli") already deleted the DEAD
tests (tests/test_job_store.py, the `translate` subcommand token, all
list/cleanup/meta.json *logic* tests). What remains extraneous and is removed here:

1. Two transitional subcommand-removal acceptance tests (CLI-03/04) that only
   assert the now-removed `list`/`cleanup` subcommands are treated as plain input
   paths. They test arbitrary positional tokens against Typer's default behavior;
   the real "no subcommands exist" guarantee is covered more robustly by
   test_help_has_no_commands_section (CLI-05), which stays.
2. Two exact-redundant CLI happy-path tests whose assertions are a strict subset
   of stronger tests in test_ephemeral.py.
3. One removed-option absence guard for `--output-format` (option removed in an
   earlier milestone; the test just asserts Typer rejects an undefined option).

Net: 210 to 205 tests, green, zero collection errors. CONSERVATIVE elsewhere:
everything exercising live behavior is kept (notably test_granularity_interactive_now_rejected,
which drives real granularity validation logic).

NOTE: the orphan tests/__pycache__/test_job_store.*.pyc files are gitignored
(*.pyc, __pycache__/) and untracked -- they are NOT a repo change and are out of
scope. pytest regenerates/prunes bytecode automatically.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@tests/test_cli.py
@tests/test_ephemeral.py
@src/book_translator/cli.py
</context>

<analysis>
## Baseline (captured at plan time)

- Test runner: `uv run pytest` (testpaths = ["tests"], asyncio_mode = "auto").
- Baseline: 210 passed, 0 collection errors (verified at plan time).

## REMOVAL TABLE (evidence-based, user-confirmed)

| # | Function (file) | Category | Evidence |
|---|-----------------|----------|----------|
| 1 | test_list_token_is_invalid_input (test_ephemeral.py) | Subcommand-removal leftover (CLI-03) | Only asserts invoke(app, ["list"]) exits non-zero and no list-subcommand text appears. Tests absence of a removed subcommand via one arbitrary token. The general "no Commands section" guarantee is held by test_help_has_no_commands_section. User confirmed removal. |
| 2 | test_cleanup_token_is_invalid_input (test_ephemeral.py) | Subcommand-removal leftover (CLI-04) | Same rationale as #1 for the `cleanup` token. User confirmed removal. |
| 3 | test_translate_success (test_cli.py) | REDUNDANT happy-path | Asserts only exit_code == 0 + "Done." in output. Success path covered by test_ephemeral.py::test_success_deletes_run_dir_and_writes_output (exit 0 + EPUB written) and the "Done." string by test_monolingual_output_gets_epub_extension + mode-dispatch tests. |
| 4 | test_translate_success_output_path (test_cli.py) | REDUNDANT happy-path | Asserts --output writes the file (out.exists(), out.read_bytes() == b"fake-epub"). test_ephemeral.py::test_success_deletes_run_dir_and_writes_output invokes with --output str(out) and asserts the SAME two facts. Exact-duplicate of the --output write path. |
| 5 | test_output_format_option_does_not_exist (test_cli.py) | Removed-option absence guard | Asserts --output-format exits code 2. --output-format was removed in an earlier milestone (no trace in current REQUIREMENTS.md); the test only verifies Typer rejects an undefined option. User confirmed removal. |

## KEEP (explicitly retained -- do NOT remove)

- test_help_has_no_commands_section (test_ephemeral.py, CLI-05) -- the robust, general
  guard that the app has NO subcommands. This is why #1/#2 are safe to remove.
- test_granularity_interactive_now_rejected (test_cli.py) -- exercises LIVE granularity
  validation (asserts valid granularities "page"/"sentence" are listed in the error).
  NOT a mere absence guard. KEEP.
- All _resolve_api_key / _resolve_base_url tests; test_translate_verbose_prints_progress
  vs test_debug_implies_progress_output (distinct flag paths); test_translate_non_verbose_does_not_print_progress;
  test_translate_parse_error_exits_1; test_api_key_flag_wins_over_both_envs_to_translate;
  all --debug diagnostics; _report_debug_failures tests; all mode/granularity dispatch +
  validation tests; test_translate_missing_required_options, test_supported_suffixes_constant,
  test_translate_unsupported_suffix, test_translate_missing_file.
- All of test_ephemeral.py except #1/#2 (run-dir lifecycle RUN-01..06 + CLI-05).
- tests/test_models.py, tests/conftest.py, tests/test_parsers.py, tests/test_translator.py,
  tests/test_assembler.py, tests/test_assembler_integration.py, tests/test_builder.py --
  all exercise live source. KEEP entirely.

CONSERVATIVE BOUNDARY: only the 5 functions above are removed. Ambiguous cases kept.
</analysis>

<tasks>

<task type="auto">
  <name>Task 1: Baseline, then prune test_ephemeral.py subcommand-removal leftovers</name>
  <files>tests/test_ephemeral.py</files>
  <action>
    Capture the pre-change baseline first: run `uv run pytest -q` and confirm it is
    green (expected 210 passed, 0 errors). If NOT green, STOP and report -- do not
    prune against a red suite.

    Then edit tests/test_ephemeral.py:

    1. Delete the function test_list_token_is_invalid_input (REMOVAL TABLE #1) in
       its entirety, including its docstring and trailing blank line.
    2. Delete the function test_cleanup_token_is_invalid_input (REMOVAL TABLE #2)
       in its entirety, including its docstring and trailing blank line.
    3. Delete the now-orphaned section comment that introduces them:
       "# --- CLI-03/04: list/cleanup run no logic, exit non-zero as invalid input path ---"
       (no tests remain under it after #1/#2 are gone).
    4. Update the module docstring (top of file): change the coverage line that reads
       "Covers RUN-01..06 and CLI-03/04/05." to "Covers RUN-01..06 and CLI-05."

    Do NOT touch any other function. test_help_has_no_commands_section MUST remain.
    Do not remove imports -- tempfile, Path, AsyncMock, MagicMock, patch, cli, app,
    TranslationError are all still used by remaining tests (grep before removing).
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && ! grep -qE "def test_(list|cleanup)_token_is_invalid_input" tests/test_ephemeral.py && grep -q "def test_help_has_no_commands_section" tests/test_ephemeral.py && uv run pytest tests/test_ephemeral.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>Both list/cleanup absence tests and their section comment are gone; module docstring updated; test_help_has_no_commands_section remains; test_ephemeral.py collects and passes (expected 10 in this file).</done>
</task>

<task type="auto">
  <name>Task 2: Prune test_cli.py redundant happy-path + removed-option tests</name>
  <files>tests/test_cli.py</files>
  <action>
    Edit tests/test_cli.py. Delete exactly three functions:

    1. test_translate_success (REMOVAL TABLE #3) -- whole function + trailing blank line.
    2. test_translate_success_output_path (REMOVAL TABLE #4) -- whole function + trailing blank line.
    3. test_output_format_option_does_not_exist (REMOVAL TABLE #5) -- whole function + trailing blank line.

    Header tidy: the section comment "# --- translate happy path and output path (mocked) ---"
    introduces ONLY functions #1 and #2. After both are removed, the next function is
    test_translate_verbose_prints_progress. Replace that stale header with
    "# --- translate progress output (mocked) ---" so the remaining verbose/non-verbose
    tests under it are labelled accurately (do not leave an orphan/misleading header).

    For #3: it sits under "# --- Mode / granularity selection tests ---", which still has
    many other tests (sentence granularity, batch-token-budget, mode dispatch, etc.).
    Leave that header in place; remove only the single function.

    Do NOT touch any other function. KEEP test_granularity_interactive_now_rejected.
    Do not remove imports -- AsyncMock, MagicMock, patch and SUPPORTED_SUFFIXES/app are
    still used by remaining tests (grep before removing).
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && ! grep -qE "def test_translate_success\b|def test_translate_success_output_path\b|def test_output_format_option_does_not_exist\b" tests/test_cli.py && grep -q "def test_granularity_interactive_now_rejected" tests/test_cli.py && uv run pytest tests/test_cli.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>All three functions gone; test_granularity_interactive_now_rejected kept; section header tidied; test_cli.py collects and passes with no import errors.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression + guards</name>
  <files>tests/</files>
  <action>
    Prove the pruning broke nothing and the safety guards hold:

    1. Run the FULL suite: `uv run pytest -q`. Expected: 205 passed (210 - 5), 0 failures,
       0 errors. If any KEPT test now fails (e.g. depended on a deleted helper), that is a
       regression -- STOP and report; do NOT "fix" it by deleting more tests.
    2. Confirm clean collection: `uv run pytest --co -q` -- no import/collection errors.
    3. Confirm the removed function names are gone from the whole tests tree:
       grep -rnE "def test_(list|cleanup)_token_is_invalid_input|def test_translate_success\b|def test_translate_success_output_path|def test_output_format_option_does_not_exist" tests/*.py
       -> no output.
    4. Confirm the retained guards still exist:
       grep -q "def test_help_has_no_commands_section" tests/test_ephemeral.py
       grep -q "def test_granularity_interactive_now_rejected" tests/test_cli.py
  </action>
  <verify>
    <automated>cd /Users/aefimov/ws/personal/book-translator && uv run pytest -q 2>&1 | tail -3 && uv run pytest --co -q 2>&1 | grep -iE "error" || echo COLLECT_CLEAN; grep -q "def test_help_has_no_commands_section" tests/test_ephemeral.py && grep -q "def test_granularity_interactive_now_rejected" tests/test_cli.py && echo GUARDS_PRESENT</automated>
  </verify>
  <done>Full suite green at 205 passed, 0 errors; collection clean; the 5 removed functions are gone; both retained guards present.</done>
</task>

</tasks>

<verification>
- `uv run pytest -q` -> 205 passed, 0 failed, 0 errors (was 210; -5).
- `uv run pytest --co -q` -> no collection/import errors.
- The 5 removed functions no longer appear in tests/*.py.
- test_help_has_no_commands_section (CLI-05) and test_granularity_interactive_now_rejected remain.
- test_ephemeral.py::test_success_deletes_run_dir_and_writes_output still proves the
  CLI success + --output write path that the removed happy-path tests covered.
</verification>

<success_criteria>
- Exactly 5 test functions removed (2 from test_ephemeral.py, 3 from test_cli.py).
- No KEPT test broken by a deletion.
- Full suite passes (205); collection clean.
- Retained guards (CLI-05 no-commands; live granularity validation) intact.
- Conservative: no other test touched; gitignored .pyc untouched (out of scope).
</success_criteria>

<output>
Create .planning/quick/260616-vb3-check-the-tests-and-remove-the-tests-tha/260616-vb3-SUMMARY.md when done.
</output>
